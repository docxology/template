type: project
description: "Project-wide soft and strong rules for source code, CI, and testing"
version: "1.0"
tags:
  - curated
  - exemplar
creator: "docxology/template"
license: "CC0-1.0"
rule_kinds:
  soft: true   # Has soft/ directory with prompt-like guidelines
  strong: true # Has strong/ directory with formal constraints
scope: "project"
