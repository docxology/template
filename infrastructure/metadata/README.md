# infrastructure/metadata

Shared publication-metadata leaves: repository-URL normalization and ebook
metadata package generation (ONIX / JSON / EPUB OPF). Consumed by publishing,
rendering, documentation, and project layers; safe for renderers to import
directly.

See [`AGENTS.md`](AGENTS.md) for the module map, invariants, and verification
commands.
