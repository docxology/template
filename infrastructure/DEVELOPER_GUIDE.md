[['str] = None\n) -> list[Path]:\n    "', '\n    Process publication metadata and write to output.\n\n    Args:\n        input_path: Source metadata YAML file\n        output_path: Destination processed metadata\n        overwrite: If True, replace existing output\n        auth_token: Optional API token for remote validation\n\n    Returns:\n        List of generated artifact paths\n\n    Raises:\n        ConfigurationError: If input file is invalid\n        FileOperationError: If output cannot be written\n\n    Example:\n        >>> process_metadata(Path("config.yaml', 'Path("out/"))\n        [Path("out/metadata.json', 'Path("out/artifacts/'], {'Code': 'python\nfrom infrastructure.core.config.loader import load_config\n\nconfig = load_config(Path(', 'projects/my_project/manuscript/config.yaml': 'author = config.author\n```\n\n**Optional Dependencies:** Guard imports with try/except and provide graceful errors:\n\n```python\ntry:\n    from some_optional_library import fancy_feature\nexcept ImportError:\n    fancy_feature = None  # Fallback path\n```\n\n### 7. Logging Standards\n\nAll infrastructure code should use the unified logger:\n\n```python\nfrom infrastructure.core.logging.utils import get_logger', 'processing': 'def process_data():\n    logger.info(', 'started': 'try:\n        # ... work\n        logger.success(', 'completed': 'Emoji in TTY\n    except Exception as e:\n        logger.error(f', 'failed': {'e}': 'raise\n```\n\n**Log Levels:** Use `logger.debug()`', 'Hierarchy': 'TemplateError` (base)\n  - `ConfigurationError`\n  - `ValidationError` → `MarkdownValidationError`', 'Context': 'python\nfrom infrastructure.core.exceptions import raise_with_context\n\nraise_with_context(\n    ConfigurationError', 'Missing required field \'author\' in config.yaml",\n    file_path=str(config_path),\n    line_number=12,\n    suggestions=[': 'dd author: Your Name to config.yaml', 'operation': 'python\nfrom infrastructure.core.pipeline import get_telemetry_collector\n\ntelemetry = get_telemetry_collector()\nwith telemetry.stage(', 'Operation': 'result = lengthy_computation()\n    telemetry.current_stage().add_metric(', 'result_size': 'len(result))\n```\n\nMetrics automatically included in pipeline reports.\n\n### 10. Security & Input Sanitization\n\n**Sanitize All User-Facing Inputs:**\n\n```python\nfrom infrastructure.core.security import sanitize_filename', 'max_length=1000)': 'raise ValueError(', 'patterns': 'Never trust** paths from external sources; use `Path.resolve()` and check within allowed directories.\n\n### 11. Contributing Changes\n\n**Before Submitting a PR:**\n\n- [ ] All tests pass (`pytest tests/infra_tests/`)\n- [ ] Coverage ≥60% for changed code\n- [ ] Type hints complete (`mypy infrastructure/<module>`)\n- [ ] Docstrings present (Google/NumPy style)\n- [ ] CHANGELOG.md updated (if user-facing change)\n- [ ] No debug prints or TODOs left in code\n- [ ] `infrastructure/SKILL.md` regenerated if adding skills\n- [ ] `docs/` updated if changing public APIs or behavior\n\n**Standard Commit Types:**\n\n- `feat:` new infrastructure feature\n- `fix:` bug fix\n- `docs:` documentation improvement\n- `test:` test addition/update\n- `refactor:` code restructuring\n- `chore:` build/CI changes\n\n### 12. Debugging Tips\n\n**Pipeline Fails Silently?** Enable verbose:\n\n```bash\nuv run python scripts/execute_pipeline.py --project code_project --core-only --verbose\nLOG_LEVEL=DEBUG uv run ./run.sh --pipeline\n```\n\n**Inspect Telemetry:** Pipeline generates `output/<project>/reports/telemetry.json`.\n\n**Check Coverage Gaps:**\n\n```bash\nuv run pytest tests/infra_tests/ --cov=infrastructure --cov-report=html\nopen htmlcov/index.html\n```\n\n**Test Infrastructure in Isolation:**\n\n```bash\n# Test module directly\nuv run python -m infrastructure.documentation.figure_manager\n\n# Use CLI entry points\nuv run python -m infrastructure.validation.cli markdown projects/code_project/manuscript/\n```\n\n### 13. Performance Considerations\n\n- Use `@log_operation` sparingly in hot paths (adds overhead)\n- Cache expensive computations with `functools.lru_cache`\n- For I/O bound work', 'Orchestrators': 'Scripts in `scripts/` delegate to `infrastructure/` or `projects/*/src/`. Never implement algorithms in scripts.\n2. **Zero-Mock Testing:** Tests use real files', 'Pipeline': "DAG defined in YAML (`pipeline.yaml`). Stage dependencies drive execution order.\n4. **Multi-Project First:** `projects/` contains independent workspaces. Infrastructure operates on any valid project uniformly.\n\n### Critical Paths\n\n```\nUser runs ./run.sh\n  ↓\nrun.sh → execute_pipeline.py (PipelineExecutor)\n  ↓\nLoads infrastructure/core/pipeline/pipeline.yaml\n  ↓\nTopological sort → stage execution\n  ↓\nEach stage script (00_*.py...) imports from infrastructure/...\n  ↓\nResults written to projects/<name>/output/\n  ↓\nStage 05 copies to output/<name>/\n```\n\n### Multi-Project Orchestration\n\n`execute_multi_project.py` uses `MultiProjectOrchestrator` to:\n\n1. Discover all active projects\n2. Run infrastructure tests once (shared)\n3. Execute each project pipeline in series or parallel\n4. Generate cross-project executive report\n\nThis is used for CI matrix builds and full repository validation.\n\n### Skill Discovery & AGENTS.md\n\nEach infrastructure submodule must provide:\n\n- `AGENTS.md` — detailed technical reference (this is what you read)\n- `README.md` — quick lookup (what's in the module)\n- `SKILL.md` — YAML frontmatter for AI agent routing\n\nThe `infrastructure.skills` module scans all subdirectories and produces `.cursor/skill_manifest.json`.\n\n---\n\n## Frequently Asked Questions\n\n**Q: Where do I put common utilities used across multiple infrastructure modules?**  \nA: Place in `infrastructure/core/` (e.g.", 'Q': "How are optional dependencies handled?**  \nA: Use try/except ImportError with clear error messages. Document optional deps in module README and pyproject.toml `[project.optional-dependencies]`.\n\n**Q: What's the policy on external API clients?**  \nA: All external calls should be wrapped in a dedicated client (e.g.", 'updated': '2026-04-27'}}]