# Orchestration

- `pipeline_manifest.py` — declares the analysis stages (sweep, simulation,
  figures, variable injection, validation, composition) and their expected
  inputs/outputs, used to drive and verify the run.
