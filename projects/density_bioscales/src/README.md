# src/

Domain logic for `density_bioscales`: gas law, liquid references, mixture density, presets, buoyancy, intervals, scenarios.

Import from tests/scripts with `src/` on `PYTHONPATH` (see `tests/conftest.py`).
