# manuscript/

Multi-section markdown for `density_bioscales`. Ordered sections: `01_` … `07_`, with `03a_`–`03d_` subsections before Methods.

- `config.yaml` — paper metadata (schema-safe keys only)
- `preamble.md` — LaTeX snippet for geometry/hyperref
- `references.bib` — minimal bibliography

Figures (`density_overview.png`, `preset_density_envelopes.png`) live under `../output/figures/` after running analysis scripts (`01_`–`03_` in `scripts/`).
