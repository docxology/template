# tests/

Zero-mock pytest suite for `density_bioscales`. One file per concern; see root `AGENTS.md` for the test→module map. `test_analysis_scripts_smoke.py` subprocess-runs all three analysis scripts.

Coverage is collected on `projects/density_bioscales/src` with `--cov-fail-under=90` in CI-style runs.
