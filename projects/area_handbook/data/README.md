# data/

Committed **input** data for reproducible builds.

- `fixtures/riverbend_area.yaml` — fictional Riverbend Metropolitan Area corpus (8 themes, 17 evidence items).

Replace or extend fixtures for new areas; keep schema aligned with `src/corpus_io.py` validation.
