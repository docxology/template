# Front Page

```{=latex}
\includegraphics[width=\linewidth]{../output/figures/masthead.png}
\vspace{0.6em}
\begin{multicols}{3}
```

**Late edition** — Lead item for the Template Gazette demonstration edition.

```{=latex}
\noindent Citation hook: \cite{template2026gazette}.
\par\smallskip
```

The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. 

```{=latex}
\end{multicols}
```



---



# National

```{=latex}
\begin{multicols}{3}
```

**Capitol and provinces**



The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. 

```{=latex}
\end{multicols}
```



---



# World

```{=latex}
\begin{multicols}{3}
```

**Foreign desks**



The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. 

```{=latex}
\end{multicols}
```



---



# Business

```{=latex}
\begin{multicols}{3}
```

**Markets and work**



The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. 

```{=latex}
\end{multicols}
```



---



# Technology

```{=latex}
\begin{multicols}{3}
```

**Tools and systems**



The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. 

```{=latex}
\end{multicols}
```



---



# Sports

```{=latex}
\begin{multicols}{3}
```

**Scores and fixtures**



The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. 

```{=latex}
\end{multicols}
```



---



# Arts & Culture

```{=latex}
\begin{multicols}{3}
```

**Stages and screens**



The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. 

```{=latex}
\end{multicols}
```



---



# Editorial

```{=latex}
\begin{multicols}{3}
```

**The leader column**



The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. 

```{=latex}
\end{multicols}
```



---



# Opinion

```{=latex}
\begin{multicols}{3}
```

**Guest essays**



The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. 

```{=latex}
\end{multicols}
```



---



# Letters

```{=latex}
\begin{multicols}{3}
```

**Reader mail**



The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. 

```{=latex}
\end{multicols}
```



---



# Local

```{=latex}
\begin{multicols}{3}
```

**City desk**



The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. 

```{=latex}
\end{multicols}
```



---



# Features

```{=latex}
\begin{multicols}{3}
```

**Long reads**



The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. 

```{=latex}
\end{multicols}
```



---



# Health

```{=latex}
\begin{multicols}{3}
```

**Wellness briefs**



The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. 

```{=latex}
\end{multicols}
```



---



# Science

```{=latex}
\begin{multicols}{3}
```

**Research notes**



The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. 

```{=latex}
\end{multicols}
```



---



# Obituaries

```{=latex}
\begin{multicols}{3}
```

**Remembrances**



The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. 

```{=latex}
\end{multicols}
```



---



# Classifieds

```{=latex}
\begin{multicols}{3}
```

**Notices**



The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. The compositor holds the column measure while Pandoc pours Markdown into LaTeX. Each folio slice maps to one manuscript file in this exemplar. XeLaTeX prints the sheet; the pipeline records the run. No mock interviews appear; every paragraph is static fixture copy for layout stress. 

```{=latex}
\end{multicols}
```



---



# Supplement: Layout and pipeline

```{=latex}
\begin{multicols}{3}
```

**How this PDF is built.** Sixteen markdown slices under `projects/traditional_newspaper/manuscript/` are discovered by `infrastructure.rendering.manuscript_discovery.discover_manuscript_files`, combined in stem order, and separated by `\newpage` inside `infrastructure.rendering.pdf_renderer.PDFRenderer` before Pandoc emits LaTeX. Tabloid paper (11 in × 17 in) and `\usepackage{multicol}` come from `manuscript/preamble.md`; column separation matches `src/newspaper/layout_spec.py` (`LAYOUT.column_sep_in`).

The masthead PNG is produced by `scripts/generate_masthead.py` calling `newspaper.masthead.render_masthead_png` during Stage 02 analysis. Optional environment variables `NEWSPAPER_TITLE` and `NEWSPAPER_TAGLINE` override banner text without editing Python.

This supplemental folio appears **after** the sixteen edition pages and **before** the glossary slice, per the template’s ordering: main numeric sections, then `S*`, then `98_*`, then `99_*` if present.

```{=latex}
\noindent For citation metadata see \cite{template2026gazette}.
\par\smallskip
```

```{=latex}
\end{multicols}
```



---



# Glossary: Newspaper and pipeline terms

Short definitions for readers of this exemplar (not a general journalism style guide).

| Term | Meaning here |
|------|----------------|
| **Folio** | One manuscript slice (`01_*.md` … `16_*.md`) that becomes a new page boundary in the combined PDF after the previous slice (plus template title front matter). |
| **Masthead** | The nameplate graphic at the top of the front page; raster asset `output/figures/masthead.png`. |
| **Slice** | Same as folio file: one ordered markdown unit in `manuscript/`. |
| **Preamble** | `manuscript/preamble.md`: LaTeX injected into the Pandoc document (geometry, `graphicx`, `multicol`). |
| **Raw LaTeX block** | Pandoc fenced region ` ```{=latex}` … ` ``` ` passing TeX through to the PDF engine. |
| **Thin orchestrator** | Script under `scripts/` that only configures paths, logging, and calls `src/newspaper/` functions. |
| **Fixture copy** | Deterministic placeholder paragraphs from `newspaper.content.fixture_copy` / `fixture_paragraph` for layout stress tests. |

The sixteen core folios are the “edition pages”; `S01_*` and `98_*` files document the system without changing the numbering of those pages.
