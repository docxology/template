# data_descriptor - AGENTS.md

Public API lives in `__init__.py`; validation behavior lives in `descriptor.py`. Keep checks deterministic and file-system independent unless a caller explicitly supplies files.
