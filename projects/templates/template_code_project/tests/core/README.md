# tests/core/ — core-cluster behavior tests

Mirrors `src/template_code_project/core/` (SUBMODULAR-CODE-2): pure
optimizer mathematics, numerical invariant builders, shared α-sweep
helpers, the single experiment-config loader, project-path resolution,
the benchmark rubric demo, manuscript `{{TOKEN}}` generation, and the
API-reference builder. See [`../AGENTS.md`](../AGENTS.md) for the testing
contract.
