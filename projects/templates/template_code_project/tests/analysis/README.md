# tests/analysis/ — analysis-cluster behavior tests

Mirrors `src/template_code_project/analysis/` (SUBMODULAR-CODE-2):
convergence experiments, stability/benchmark reporting, publishing
helpers, the workflow orchestrator, and the auxiliary script smoke tests
(`scripts/` delegate to every cluster, so their smoke tests live with the
orchestration cluster). See [`../AGENTS.md`](../AGENTS.md) for the testing
contract.
