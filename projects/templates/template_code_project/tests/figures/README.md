# tests/figures/ — figures-cluster behavior tests

Mirrors `src/template_code_project/figures/` (SUBMODULAR-CODE-2):
matplotlib figure generators (convergence, sensitivity, complexity,
stability/benchmark), the shared styling config (`viz_config`), and the
runtime-helper shim (`_common`). See [`../AGENTS.md`](../AGENTS.md) for the
testing contract.
