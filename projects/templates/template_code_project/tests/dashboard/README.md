# tests/dashboard/ — dashboard-cluster behavior tests

Mirrors `src/template_code_project/dashboard/` (SUBMODULAR-CODE-2):
dashboard payload computation/defaults, Plotly panel assembly, and the
`build_dashboard` CLI facade (including the invariants-through-dashboard
integration covered by `test_invariants_and_dashboard.py`). See
[`../AGENTS.md`](../AGENTS.md) for the testing contract.
