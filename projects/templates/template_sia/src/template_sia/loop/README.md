# loop/ — self-improvement loop orchestration

Layer-2 project adapter for the shared SIA harness (`infrastructure.sia`
state machine), typed settings, the fail-closed approval contract, and the
deterministic reference target agent. See [`AGENTS.md`](AGENTS.md) for the
module map.
