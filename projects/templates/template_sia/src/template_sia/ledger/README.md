# ledger/ — artifact manifest and generation records

Deterministic artifact manifests (hashes + metadata) for SIA loop outputs and
loading of recorded generation records from disk. See [`AGENTS.md`](AGENTS.md)
for the module map.
