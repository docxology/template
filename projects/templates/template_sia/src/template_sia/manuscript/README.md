# manuscript/ — tokens, variables, and reports

`{{SIA_*}}` token computation (core + metric tables), render-time manuscript
variable merging/hydration, and loop summary reports. See
[`AGENTS.md`](AGENTS.md) for the module map.
