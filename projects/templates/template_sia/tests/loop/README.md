# tests/loop/ — loop-cluster behavior tests

Mirrors `src/template_sia/loop/` (SUBMODULAR-SIA-1): the project adapter,
settings loading, the approval contract, and the reference target agent
(internals via `test_src_reference_agent.py`, CLI behavior via
`test_reference_agent.py`). See [`../AGENTS.md`](../AGENTS.md) for the
testing contract.
