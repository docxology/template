# Simulation (pymdp track)

Sophisticated-inference simulation of a T-maze Active Inference agent.

- `tmaze_model.py` — the generative model (A/B/C/D matrices) for the T-maze.
- `si_runner.py` — deterministic sophisticated-inference rollout.
- `logging_utils.py` — structured, reproducible run logging.
