# scripts/ — template_experiment_tree

Thin orchestrators only: `init_tree.py`, `record_experiment.py`,
`report.py`, `z_generate_manuscript_variables.py` (hydration entrypoint).
All logic in `../src/template_experiment_tree/`.
