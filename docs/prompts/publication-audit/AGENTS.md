# Publication-audit skill folder

`SKILL.md` is the routable workflow descriptor. Keep its commands aligned with
`infrastructure.validation.publication` and update the generated skill index
after changes.
