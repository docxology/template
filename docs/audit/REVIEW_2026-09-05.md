# Adjacent-surface review and hardening — 2026-09-05

## Scope and baseline

Follow-up campaign after the 2026-09-04 hardening (PR #52, merged at
`4521726a185c6d1217b595939d854b485ae90abc`). The session re-verified the
baseline live before editing: clean working tree, HEAD equal to
`origin/main`, `git diff 56c56b2cc 4521726a1` empty (the merge tree matches
the reviewed source tree), hosted main CI run
[`33941394683`](https://github.com/docxology/template/actions/runs/33941394683)
recorded `success` (65 jobs, three expected optional skips) after PR run
[`33938657975`](https://github.com/docxology/template/actions/runs/33938657975).
GitNexus has no index for this checkout, so review used direct source
reading: nine read-only scout lenses audited the surfaces adjacent to the
2026-09-04 fixes (backlogs and their parser, readiness cost model, rendering
and accessibility, containment and exports, config validation, test
isolation, doc-to-code claims, the clean-checkout rehearsal unblock path,
and receipts/changelog reconciliation). Entry-doc claims (worker caps,
floors, flags, `--check` commands) were re-verified against code with no
mismatch found. The campaign changed no dependencies, no lockfiles, no
coverage floors, and no supported interfaces.

## Changes and negative controls

| Finding | Implemented behavior | Regression evidence |
| --- | --- | --- |
| Beamer slides remapped only `≥`; other Unicode dropped silently from projected frames | Slides glyph table routes relations, arrows, operators, and Greek through the math font via the shared protected-block tokenizer; every replacement is wrapped in `\texorpdfstring` (moving-argument safety); graphics filename arguments and protected code regions stay byte-identical | Transform unit tests incl. a Unicode `\includegraphics` filename, real accessible-slide suites, and a real XeLaTeX-class probe for the filename case |
| Web issue scanner validated anchors but not `<img src>`; missing figure files shipped broken | `deployed_web_link_issues` now checks local image sources with the anchor rules (missing, escape, unsafe bytes, unsupported schemes fail closed; `http(s)`, `mailto`, `tel`, `data`, `blob` pass) | Scanner tests: missing sibling figure reported, present/remote/inline accepted, `javascript:` rejected |
| Pandoc wrote the final per-section and combined HTML in place | Publish-by-rename from exclusive temporary targets; existing page permissions are re-applied before replace; `OSError` publishes raise `RenderingError` with context; hidden `.html.tmp` orphans are renderer-owned and cleaned with the stale-artifact pass | Permission-preservation test (a `0600` page stays `0600`), full web-render suites |
| TeX aux repair used a predictable `.tmp` name with a symlink-following write; combined-markdown temp likewise | Both writes use the confined atomic writer (`atomic_write_text_confined`) | Confined-writer contract tests; compile + affected rendering suites |
| Favicon write followed planted symlinks | Refuses symlinks, writes through an exclusive temp, warns on failure | Favicon tests in the web asset suites |
| Export `manifest.json` written in place; failed copy left an orphan partial bundle; a stale real directory at `latest` failed a complete export | Manifest is atomic (exclusive temp, fsync, rename); the bundle directory is removed on any copy/manifest/latest failure (allocated by exclusive mkdir, so removal cannot touch caller state); a stale real directory in the `latest` slot is replaced | New export tests: orphan removal after a symlink-escape failure, manifest temp cleanliness, stale-directory replacement; the documented hash-copied-bytes manifest contract is preserved (`test_export_manifest_hashes_actual_copied_bytes` still passes) |
| Checkpoint output-tree digests followed file symlinks | Symlinks are excluded from the digest walk | Planted-symlink test: external bytes never enter the digest |
| `experiment:` malformed values crashed with raw tracebacks; non-integral integers truncated silently; flat/ragged matrices vanished silently | Every malformed value degrades to the exemplar default with a field-naming warning (scalar `experiment:` node, `None`, non-numeric strings, booleans, nested lists, ragged/empty matrices, non-finite floats, non-integral integers, dimensions < 1); absent keys stay silently default | 17-case malformed-input regression class plus the pre-existing pinned fallback tests |
| Rehearsal determinism was asserted but never enforced; a blocked receipt could exit 0 | The overall receipt blocks when the two runs' digests differ; `runs >= 2` is enforced; the CLI exit gates on receipt status *and* validation | Two real fresh-clone rehearsal tests (identical digests → pass/exit 0; `echo $$` digests → blocked/exit 1) |
| Backlog gate allowed `completed` rows while the prose and `BacklogStatus` forbade them | `completed` removed from `ALLOWED_BACKLOG_STATUSES`; the 17 closed root rows moved verbatim to [BACKLOG-CLOSURE-2026-09-05.md](BACKLOG-CLOSURE-2026-09-05.md); root TO-DO.md is now future-work-only with its four blocked-external rows and receipts intact | Gate passes (`25` files, `16` stable identifiers, strict); negative control: a `completed` row fails `invalid_status` |
| Tests touched shared state: a real `uv sync` against the repo root; an unscoped env var; a git-dependent assertion | Success-path `uv sync` runs against a synthetic zero-dependency workspace; `MMLINT_TEST_COUNTER` is monkeypatch-scoped; the tracked-count test skips outside real git checkouts | Isolation slices green; CLAUDE.md now documents the one-exemplar-tree-per-process rule and its mechanism; follow-up tracked as `TEST-ISOLATION-SYSPATH-1` |

An independent post-implementation review agent re-derived the row census
from git history (17 rows, byte-identical in the closure record), confirmed
all five preserved contracts, and its findings were fixed before staging.
A second-opinion review surfaced three real regressions in the first cut —
the rehearsal exit-code coupling, graphics-filename corruption, and lost
HTML permissions — each fixed with a regression test.

## Deferred-verify closure

The 2026-09-04 pixel-capture deferral is closed. Compositor screenshots of a
real rendered accessible deck were taken at 390×844 and 1200×800 with a
managed Chromium: narrow scroll-mode content measures **374.4 px** with no
horizontal overflow, MathJax loaded with zero error nodes, cross-reference
links render, and presentation mode retains its width limit (1053.3 px).
Receipts: `/tmp/template-pixcap-shots/` (session-local) plus the DOM
measurements recorded in `STATUS.md` row `STATUS-RENDER-RECOVERY-1`.

## Clean-checkout rehearsal status

`CLEAN-CHECKOUT-MAJ-1` remains correctly blocked-external: no workflow
invokes the rehearsal on hosted Linux, a `darwin` receipt cannot satisfy the
row's own acceptance, and the `uv sync --locked --offline` first command
requires a pre-warmed runner cache. The machinery de-risked this session:
determinism is now enforced locally, and the Git 2.51+ `clone --revision`
floor is documented.

## Verification

- Full infrastructure suite: **10,744 passed, two skipped**, 60% floor met, <!-- noqa: drift-counts -->
  exit 0 (the standing numerical-overflow warning remains visible).
- Regression tier: 55 passed. Real accessible-slide suite: 53 passed. <!-- noqa: drift-counts -->
- Affected slices (publishing, web, beamer, checkpoint, documentation,
  core): 1665 + 135 + 81 passed across the campaign; each exemplar tree ran <!-- noqa: drift-counts -->
  in its own process.
- Exemplar coverage matrix: all 24 public exemplars re-measured via
  `counts.py --verify-coverage --write`; every floor held (`0 failed`);
  coverage provenance refreshed; `counts.py --check` reports COUNTS.md in
  sync; `status_evidence.py --write` + `--check` green.
- Gates: drift (strict), documentation lint, claim bindings, public
  contract (strict), tracked containment (projects/fonds/rules/tools),
  tracked generated artifacts, tracked + staged secrets, backlog (strict),
  STATUS freshness — all pass. Ruff lint/format and mypy (1,606 files)
  clean.
- Ready receipt: the one-invocation readiness matrix was run against a
  disposable clone of the publication branch and is recorded on the PR.

## Known boundaries (documented, intentionally unfixed)

- Exemplar `src`/`tests` top-level package collisions make multi-tree
  pytest processes unsafe; restructuring is tracked as
  `TEST-ISOLATION-SYSPATH-1` instead of silently widened.
- Export manifests record the copied bytes' digest (documented
  hash-copied-bytes contract); collect-time digests are not compared.
- The `latest`-slot replacement removes renderer-named state inside the
  documented caller-trusted destination.
- The shared `\passthrough{...}` tokenizer does not match nested braces
  (pre-existing, shared with the PDF path).
- `run_matrix` ignores unknown run-entry keys silently; negative
  `--parallel` values degrade to serial (INFO).
- The GitHub repository description is stale ("16-stage/25 exemplars");
  fixing it requires owner settings access (external).

## Continuation: skip-free optional-tool coverage (2026-09-06)

The full-suite receipts carried exactly two skips: complementary calibre
pairs in `tests/infra_tests/rendering/test_docx_epub_fallbacks.py` — an
absence-path test that ran only where calibre is missing, and presence-path
tests that ran only where it is installed, so every environment skipped
exactly two. Both pairs are now environment-adaptive contract tests that
always run:

- The MOBI absence contract is exercised through an explicitly unresolvable
  `ebook-convert` binary name — the same `shutil.which` resolution the
  renderer performs — so the typed failure and install hint are verified in
  every environment.
- The ebook-generation pipeline test asserts the documented per-format
  degradation contract where calibre is absent (EPUB and DOCX render via
  pandoc, MOBI fails closed, stage exits 0 with partial success) and the
  full-success assertions where it is present. The degradation branch was
  previously uncovered in CI.
- Converted file: 48 passed, zero skipped. Full-suite expectation: zero <!-- noqa: drift-counts -->
  skips; verified in this continuation's infrastructure run.
- Publication: continuation merged through the same reviewed-PR flow
  (hosted CI green, main parity verified at merge).
