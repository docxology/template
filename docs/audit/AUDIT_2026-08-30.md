# Audit — 2026-08-30 (herdr audit lane, repo: /Volumes/external_drive/Git/template, working alias /Users/4d/Documents/GitHub/template)

Lane: herdr audit lane · branch `main` · HEAD `ecdab23db`

## Shared-checkout context (governs this whole audit)

The working tree was **mid-refactor by concurrent sibling agents** for the entire lane:
- ~542 modified + ~653 deleted tracked files (a `manuscript/` -> `docs/manuscript/` migration plus a `template_formal` AGENTS.md authoring wave), 11 untracked files under `projects/templates/template_formal/`.
- Multiple concurrent `git push origin`, `git gc/repack`, and `git lfs pre-push` processes observed running.
- A stale `index.lock` (0 bytes, 14:19) was present mid-session; git plumbing commands (`git diff`, `git status -uno`) took 2-19 minutes against the 22 TB external drive under this load. Left untouched (stale-lock removal is destructive; sibling writers were active).
- One sibling agent had already applied the same timeout fix I prepared (see CRITICAL-1) at 15:35 while I was diagnosing.

Consequence: **audit + report only. No commits, no doc rewrites, no wide edits** — every target file was moving under someone else's pen.

## CRITICAL

1. **Flaky smoke-gate timeouts under external-drive + concurrent-load conditions** — `tests/infra_tests/git_hook_smoke/test_gate.py:62,75` (60s subprocess caps) and `test_tracked_generated_artifacts.py:81,85` (240s full-tree blob scan). Measured: `python -m infrastructure.validation.cli --help` = 17.6s (warm) -> 41-78s (loaded); cold `uv run` import = 40-57s. The pipeline-smoke gate run I executed (1723s wall) failed twice on these, once per file, purely on timeouts — no real assertion failure.
   Status: the test_gate.py half was fixed (sibling: `timeout=180` + `@pytest.mark.timeout(240)` guards, with measurement comments). The secrets-scan test still exceeded 240s once under peak load (~23s quiet, per its own comment). **Root cause is environmental (22 TB external APFS volume + 4+ concurrent git processes), not code.** Recommend: keep generous caps as done; do not chase with ever-larger budgets. Verified fix: `uv run pytest tests/infra_tests/git_hook_smoke/test_gate.py` -> 5 passed (133s) after the fix, vs timeout-fail before.

## MAJOR

2. **Docs drift risk during the manuscript-path migration** — the in-flight diff updates root AGENTS.md/CLAUDE.md to `docs/manuscript/`, and `infrastructure.core.project_paths.resolve_manuscript_dir` (infrastructure/core/project_paths.py:82) now supports both layouts. But sibling docs still mix conventions (e.g. `projects/templates/template_formal/docs/manuscript/AGENTS.md` render examples reference `manuscript/`, `projects/AGENTS.md` says `projects/{name}/manuscript/`). Not fixable in this lane without colliding with the migrating agent; flagged for the lane that owns the migration to sweep remaining prose after it lands.
3. **Stale `.git/index.lock` + git performance** — concurrent agents + external drive make even read-only git ops take minutes; `core.fsmonitor=false` and `core.untrackedCache=false` on a repo this size on this volume is a measured performance defect (status = 144s). Recommend (as a follow-up for a quiet checkout): evaluate enabling fsmonitor/untrackedCache, and document the never-concurrent-writers rule in the fleet dispatch skill. Not applied: repo policy changes mid-migration are out of lane scope.

## MINOR

4. **Backlog hygiene is good** — TO-DO.md: all 10 root rows are `completed` (6) or `blocked-external` (4) with full acceptance-command contracts; nothing stale or unowned. No fix needed.
5. **TODO/FIXME debt in infrastructure/ is near zero** — 28 string matches, all in docstrings/tooling that *manage* TODOs (backlog normalizer, security gate scanning for TODO markers), none are unresolved code TODOs. No fix needed.
6. **Untracked `template_formal` AGENTS.md files** (11 files, untracked) — part of the sibling documentation wave; a real tracked gap per that wave's own note ("every other exemplar carries one"). Left for its owner to commit.

## Verified-clean checks (no fabrication, all executed)

- Public roster consistency: `docs/_generated/active_projects.md` includes `templates/template_formal`; `infrastructure/project/public_scope.py:27` lists it — consistent.
- `uv run python -m infrastructure.validation.cli --help` exits 0 (multiple runs).
- No-mock policy spot-check of smoke tests: real subprocesses, real scans, negative-control tests present (e.g. fake-repo secret fixture in test_tracked_generated_artifacts.py).
- CI graph, coverage gates (60% infra / 90% project / 75% union) documented and wired; not re-run in this lane (full infra suite exceeds lane budget on this checkout: pipeline-smoke alone = 28 min).

## Gate results (before/after)

- Before: `stage_01_test.py --infra-only --infra-scope pipeline-smoke` -> **EXIT 1** (1 failure > tolerance 0): timeout in `test_validation_cli_help_returns_zero` (and earlier run also hit `check_tracked_all` 60s).
- After (targeted): `pytest tests/infra_tests/git_hook_smoke/test_gate.py` -> **5 passed** (133s) with the 180s budgets.
- Residual: `git_hook_smoke` as a whole -> 6 passed / 1 timeout (`test_current_repo_has_no_high_confidence_tracked_secrets`, 240s budget exceeded under peak concurrent git load; passes quiet). Environmental; recommendation in CRITICAL-1.

## Files changed by this lane

- None committed; none left dirty by me (the one file I prepared to edit had already been fixed by a sibling — confirmed on re-read before write). This audit file is the only artifact I authored, placed at repo root per lane instructions.

## Addendum 3 (gate-verify lane)

Re-verification run 2026-08-30 18:55-21:42 PDT, serial, from repo root. Load ~3-11 (well below the audit's 18). Raw log: /tmp/gate_verify_report.txt.

| # | Check | Exit | Duration | Result |
| - | ----- | ---- | -------- | ------ |
| 1 | pytest tests/infra_tests/git_hook_smoke/test_gate.py -v --timeout=900 | 1 | 942s (3:42) | 4 passed, 1 FAILED - `test_validation_cli_help_returns_zero` timed out at its 60s subprocess cap (62.03s call); NOT the M-1 test. `test_discover_projects_finds_templates` (the M-1 timeout site) PASSED at 53.65s. |
| 2 | scripts/audit/check_template_drift.py --strict | 1 | 8117s (2:15) | 4 warnings (strict escalates): dead_link x3 (template_code_project + template_prose_project `docs/manuscript/AGENTS.md` -> `../docs/rendering_pipeline.md` / `../docs/troubleshooting.md`), template_formal PUBLISHING-STATUS block stale. Doc-drift content, not infra. |
| 3 | scripts/audit/check_tracked_all.py | 0 | 65s | projects/fonds/tools/rules: all clean |
| 4 | scripts/audit/check_tracked_generated_artifacts.py | 0 | 490s | No prohibited generated artifacts or evidence-budget violations (definitive result; never completed during the audit) |
| 5 | python -m infrastructure.skills check | 0 | 111s | ok |
| 6 | python -m infrastructure.skills check-all-exports | 0 | 84s | 0 `__all__` violations |
| 7 | ruff check infrastructure scripts | 0 | 17s | All checks passed |

Verdict: the exact M-1 timeout (`test_discover_projects_finds_templates`, tomllib read at infrastructure/project/metadata.py:62) does NOT reproduce at normal load - it passed in 53.65s. However, smoke-gate flakiness under this volume persists in a sibling test: `test_validation_cli_help_returns_zero` hit its own 60s subprocess cap (consistent with CRITICAL-1's environmental diagnosis; audit measured that same CLI at 17.6-78s depending on load). All static guards and full-tree scans now pass; the only non-passing gates are one environmental smoke timeout and content-level drift warnings belonging to the docs-stages lane's scope.
