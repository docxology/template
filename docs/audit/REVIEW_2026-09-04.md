# Public template improvement review — 2026-09-04

## Scope and baseline

Local implementation against `e5b3dfb2aabb519f17416eefe43a75a5da5caa10`.
The initial working tree was clean and hosted CI passed on that commit. This
review covers public infrastructure and canonical exemplars. It does not
publish, change dependencies, modify private sidecars, or alter coverage floors.

The first disposable-checkout infrastructure run passed 10,470 tests, failed <!-- noqa: drift-counts -->
seven, and skipped two. Five failures were portable-fixture defects reproduced
independently: a copied macOS Python binary lacked its relative shared library,
and Python 3.14 ZIP fixture timestamps underflowed in Los Angeles. The other two
were interference from concurrently running project verification in the same
checkout; both passed independently against the untouched working tree.
Final infrastructure and public-project runs therefore use separate checkouts.
The initial temporary-directory coverage result was invalid because repository
coverage configuration excludes that directory; final coverage runs outside it.

## Changes and negative controls

| Finding | Implemented behavior | Regression evidence |
| --- | --- | --- |
| Rendering owners near the size limit | Split figures, table structure/cell metrics/width solving, Beamer preparation, and web assets/links/writes into cohesive modules; retain old helper imports | All 54 original AST/table function and class bodies compare identically; callable re-exports and acyclic imports verified |
| Predictable HTML/TeX temporary paths | Shared confined atomic writer uses exclusive temporary files; HTML retains permissions and unchanged mtime | Planted temporary symlinks leave unrelated files intact; concurrent writes remain complete; encoding failure leaves no owned temporary |
| Unsupported URLs with empty paths | Check URI scheme before accepting empty paths/fragments | `javascript:` and `data:#fragment` are rejected |
| Invalid checkpoint JSON types | Validate counters, finite times, stage results, and digests; invalid resume data is unavailable | Wrong types, booleans, nonfinite numbers, and incomplete stages are rejected without crashing resume |
| Checkpoint placement and partial writes | Executor passes resolved project directory; failed serialization/digest reads/writes preserve prior checkpoint | Qualified lifecycle placement and prior-file preservation verified |
| Export escapes and scan/copy race | Validate selected roots and read through held no-follow descriptors, including during copying | File, bucket, and root symlink swaps reject outside reads; normal internal artifact links and managed project leaf links remain supported |
| Same-second bundle reuse | Allocate exclusively, hash copied bytes, update `latest` atomically after success | Distinct bundles and unchanged `latest` on failure verified |
| Platform-sensitive test generators | Symlink-backed venv and explicit ZIP entry dates, with fixture errors propagated | Original five failures corrected; complete three-file slice: 99 passed, one deselected <!-- noqa: drift-counts --> |

The export destination is caller-selected and trusted. Source confinement does
not claim protection against hostile concurrent mutation of destination
ancestors. Confined export reads require no-follow descriptor-relative opens;
unsupported platforms fail closed.

## Verification

The numbers below are receipts from this dated review. Current repository
totals are maintained in [COUNTS.md](../_generated/COUNTS.md).

- Full infrastructure suite: 10,530 passed, two skipped, and 83.00% coverage <!-- noqa: drift-counts -->
  against the unchanged 60% floor. Two assertions initially failed solely because generated
  infrastructure-file and publishing-test counts were stale; their post-refresh
  recheck passed both assertions. The single warning is the existing numerical
  exponential-overflow test.
- Regression suite: 55 passed before and after implementation. <!-- noqa: drift-counts -->
- Real accessible slides: 53 passed without skips; covers Pandoc, Beamer, <!-- noqa: drift-counts -->
  Reveal, citations, tables, math, figures, geometry limits, and paired cleanup.
- Real archive/core slide rendering: 12 passed without skips. <!-- noqa: drift-counts -->
- Real Beamer temporary-symlink test: failed before correction, passed after.
- Golden archive and accessible rendering: normalized HTML, TeX, and web bytes
  identical; PDF extracted text and page boxes identical. Normalization removes
  only the per-run fixture root; binary PDF metadata equality is not claimed.
- Dependency audit: root plus every canonical public exemplar, 25 audits
  passed using the existing lockfiles and configured ignore policy.
- Bandit MEDIUM/HIGH gate passed; existing LOW findings remain visible.
- Source methods contracts: all 24 public exemplars passed.
- Clean export, installation, and import smoke checks: all 24 public exemplars
  passed. Strict source publication audit: zero findings.
- Eighteen repository gates passed, including full Ruff lint/format, mypy over
  1,606 source files, documentation lint, tracked-data containment, public
  contracts, claim bindings, module sizes, no-mocks, API references, and skill
  exports. No API-reference or skill-export regeneration was necessary.
- Generated-count implementation tests: 88 passed after refreshing the <!-- noqa: drift-counts -->
  generator date. Owner regeneration and `--check` passed: 804 infrastructure
  Python files and 842 publishing tests. Both former drift failures passed. <!-- noqa: drift-counts -->
  Status source receipt and freshness checks passed. The complete documentation
  discovery/status test slice passed all 30 tests. <!-- noqa: drift-counts -->
- Fresh-context adversarial review found the export scan/copy race; its original
  reproduction now fails closed. Final reviewer probes passed 20 tests and <!-- noqa: drift-counts -->
  reported no remaining actionable finding within the stated boundary.

Both baseline and final public-readiness matrices passed 23 exemplars; active
inference exceeded their default 1,200-second outer deadline. Its isolated
retry completed successfully in 1,541.526 seconds using the native verifier:
898 passed, one skipped, and 92.67% coverage. All twelve coverage groups and <!-- noqa: drift-counts -->
postflight checks completed; Stage01 validated the fresh run-bound receipt
against its independently read coverage database.

All 24 public exemplar verification commands therefore passed across the
matrix and targeted retry: 6,307 tests passed and five were skipped. Every <!-- noqa: drift-counts -->
project met the existing 90% coverage floor. The skipped tests are:

- Pools: two executor checks require unavailable `timeout`/`gtimeout`; `jq`
  is present. A direct probe reproduced these skips.
- Search: two optional LLM checks cannot import `infrastructure.llm` because
  the project environment lacks `requests`. A fresh native Stage01 run with
  `PYTEST_ADDOPTS=-rs` confirmed the reasons, while its manuscript checks passed.
- Active inference: one typography check requires the combined manuscript TeX
  produced by Stage03 rendering, which was absent from this Stage01 run.

Skipped tests are excluded from passing totals. The longer timeout is a local
verification invocation in the first pass. The continuation below corrects
the default supervision budget based on that measured run.

## Continuation: readiness and live browser verification

The public readiness deadline now defaults to 7,200 seconds per project,
300 seconds longer than the unchanged native verifier budget. Both policy
values live in `infrastructure.core.pytest_profiles`; the previous reporting
import remains compatible. Explicit shorter deadlines remain supported.
Readiness rejects empty, incomplete, duplicate, unexpected, and contradictory
reports. JSON labels counts as projects and records each executed timeout.
The targeted readiness tests failed before the implementation and passed after;
real subprocess checks cover success and forced timeout. A fresh reviewer also
confirmed detached-child termination. The complete related orchestration,
reporting, verifier, and readiness slice passed 138 tests. <!-- noqa: drift-counts -->
The full public matrix was not repeated in this continuation; its successful
project-level receipts above remain separate from the new policy tests.

Two further real-Pandoc probes reproduced unrelated-file overwrites through
per-section and combined Markdown temporary symlinks. Per-section preprocessing
now uses a private temporary directory, cleaned on success and failure; combined
Markdown uses the confined atomic writer. Both planted links and unrelated
bytes survive rendering. The web/write suite passed 71 tests; an independent <!-- noqa: drift-counts -->
review passed the complete renderer file and found byte-identical relative-image,
neighboring-link, math, and formalism output against the earlier implementation.

Interceptor connected to Chrome during this continuation. Real generated decks
were loaded in same-origin frames at 390 × 844 and 1,200 × 800 CSS pixels. The
narrow view exposed an additional width clamp before Reveal's scroll-mode scale:
content occupied only 152.1 pixels. Removing that clamp specifically inside
`.scroll-page` restored 374.4 pixels, without horizontal page overflow. Normal
presentation mode retains its width limit. The regenerated fixture reproduced
the correction, and fresh source/cascade review found no actionable concern.

Live Chrome probes verified loaded math without MathJax error nodes, semantic
table headers, loaded figures with descriptions, skip-link focus transfer, and
both companion links opening the canonical HTML reader. The canonical narrow
reader had no horizontal overflow and its figure fit within 358 pixels. A
browser-dispatched right-arrow event advanced the wide deck; narrow view uses
Reveal's scroll mode, where each of the four slides was reached and fit fully
inside the viewport. These are browser DOM/layout and event-handling checks,
not a screen-reader or trusted hardware-keyboard certification.

The real slide suite passed 53 tests after the CSS correction. <!-- noqa: drift-counts -->
Golden comparison confirms the only output change is the intended accessible
Reveal CSS/comment; archive outputs, web/TeX, and PDF text/page geometry remain
identical. Native public-scope Ruff lint/format and mypy over 1,606 source files
passed. An overbroad exploratory format command included generated outputs and
skill examples outside CI scope; the declared CI scope passed without modifying
those files. Repository containment, capability, contract, claim-binding,
no-mocks, module-size, and API-reference checks also passed.

[DEFERRED-VERIFY] Pixel-accurate screenshot review remains unavailable:
Interceptor's DOM renderer omitted iframe/text content, and compositor capture
returned an `activeTab` permission error. The screenshots are not accepted as
pixel evidence. A browser session with compositor capture permission can finish
that visual check; the earlier blanket claim that no extension was connected
is superseded by the live Chrome measurements above.

The export explanation is consolidated in its detailed `AGENTS.md` contract;
a redundant README addition was removed to retain the already measured
documentation support input. Coverage provenance was not relabeled or bypassed. Owner regeneration updated
the collected project-infrastructure test count from 608 to 624 after the new
readiness cases. <!-- noqa: drift-counts -->

Dependency and lock files remain unchanged. One reviewer command resynchronized
the local editable root installation; subsequent verification used
`uv run --frozen --no-sync`. No publication or remote mutation occurred during those local passes. The user
subsequently authorized an informative PR and merge to main. Publication is
gated by a fresh source review, repository hooks, containment checks, and hosted
CI; its final commit and merge outcome are recorded by the PR.

## Publication review

Fresh release reviews independently passed 108 security/recovery tests and
34 readiness/HTML tests without an actionable code finding. <!-- noqa: drift-counts -->
Review did identify stale PR-template stage numbers and non-strict commands;
the template now links the generated stage map and uses strict readiness.

The release infrastructure sweep exposed a missing-input compatibility regression
in the new web preprocessing path: it propagated `FileNotFoundError` instead of
`RenderingError`. The renderer now wraps preparation I/O failures with the
public error type, source context, and chained cause. The original failure test
and two new real missing/directory input probes failed before the fix and passed
after; temporary storage is removed on both paths. The complete affected web
slice passed 75 tests, and fresh review independently passed all three error <!-- noqa: drift-counts -->
probes.

Final release infrastructure verification: **10,553 passed, two skipped**, 83.04% coverage against the unchanged 60% floor. The existing numerical overflow warning remains visible. <!-- noqa: drift-counts -->

## Reproduction commands

Use isolated checkouts for infrastructure and public-project verification so
project artifact regeneration cannot invalidate infrastructure manifest tests.
Keep coverage checkouts outside paths excluded by `pyproject.toml`. Inventory
and containment gates used disposable staged checkouts so new files participated
in Git-based checks. The first pass hash-compared its changed source/test files with the infrastructure
snapshot. The continuation used a refreshed isolated snapshot for gates and
recorded a new identity receipt for every changed file. Project-test receipts
remain bound to the first-pass project snapshot.

```bash
uv run pytest tests/infra_tests/ -n 2 --dist loadscope --benchmark-disable --cov=infrastructure --cov-fail-under=60 --timeout=120 -m "not requires_ollama and not requires_docker and not network and not slow and not bench and not benchmark and not performance"
uv run pytest tests/regression/ -q --no-cov --timeout=120
uv run python scripts/gates/public_readiness.py --json --project-workers 2 --timeout 7200
uv run python scripts/pipeline/stage_01_test.py --project templates/template_active_inference --project-only --profile release
uv run pytest tests/infra_tests/rendering/test_slides_accessibility.py -m slow -q --no-cov --timeout=120
uv run pytest tests/infra_tests/rendering/test_beamer_atomic_write.py -m slow -q --no-cov --timeout=120
uv run python scripts/docgen/counts.py --check
```
