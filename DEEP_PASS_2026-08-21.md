# DEEP_PASS_2026-08-21 — Repository Deep Assessment & Improvement Pass

Scope: full-repo assessment (architecture, code quality, tests, docs, CI, security,
dependency health, broken links, dead code, TODO debt) with minor/medium fixes
implemented and major findings scoped. All measurements below were executed live in
this checkout on 2026-08-21 unless marked otherwise.

## Executive summary

Repository health is **strong**. Every enforced gate I ran came back clean:

| Gate | Command | Result |
| --- | --- | --- |
| Ruff (full public lint surface) | `python -m infrastructure.project.public_scope lint-paths` + `ruff check` | All checks passed |
| mypy (import-safe source paths, 1559 files) | `public_scope source-paths` + `mypy` | Success: no issues |
| No-mocks lexical gate | `scripts/audit/verify_no_mocks.py` | Exit 0 |
| No-mocks semantic inventory | `--inventory --max-dependency-replacements 0` | **Was FAIL (debt 2) -> now Status: clear (0)** — fixed this pass |
| Template drift | `scripts/audit/check_template_drift.py` | no drift detected |
| Documentation linter (mermaid + cross-link) | `scripts/audit/lint_docs.py` | exit 0 (268 mermaid blocks checked) |
| Backlog contract | `scripts/audit/check_backlog.py` | 25 files, 22 stable IDs, 0 errors/warnings |
| Confidentiality guards (projects/fonds/rules/tools) | `scripts/audit/check_tracked_all.py` | clean |
| Tracked-secrets scan | `scripts/audit/check_tracked_secrets.py` | no credentials found |
| Generated-artifact guard | `scripts/audit/check_tracked_generated_artifacts.py` | clean |
| Skills manifests | `infrastructure.skills check` / `check-all-exports` | ok / 0 violations |
| API reference freshness | `scripts/docgen/api_reference.py --check` | up-to-date (25 packages) |
| Bandit (`bandit.yaml`) | over `infrastructure/`, `scripts/` | no findings reported |

The single enforced-gate failure found (semantic dependency-replacement debt of 2)
was fixed and verified during this pass. Remaining observations are advisory
(line-count warnings under an expiring ratchet) or local-environment state
(unmanaged private mirror directories), detailed below.

## Findings

### Fixed (Medium)

**M1 — Semantic dependency-replacement debt 2 > ceiling 0**
(`tests/infra_tests/project/test_workspace_branches.py`)
- Evidence: `uv run python scripts/audit/verify_no_mocks.py --inventory --max-dependency-replacements 0`
  exited FAIL with `dependency_replacement: 2`; both `monkeypatch.setattr`
  operations lived in this one file (`subprocess.run` stub; `workspace.run_uv_command`
  replacement).
- Fix: replaced both stand-ins with real invocations:
  - OSError branch now calls `run_uv_command` with a guaranteed-missing executable
    name (real `subprocess.run` raises; helper returns 1).
  - sync/update failure branches now chdir into an empty tmp dir and let the real
    `uv` binary fail with "No pyproject.toml found" (verified return code 2).
- Verification: `verify_no_mocks.py --inventory --max-dependency-replacements 0`
  -> `dependency_replacement: 0`, `Status: clear`;
  `uv run pytest tests/infra_tests/project/test_workspace_branches.py -q` -> 6 passed.
- Commit status: **left uncommitted deliberately** — see "Concurrent-writer note".

### Minor / advisory (no action required or deferred)

**m1 — Module line-count warnings** (`scripts/gates/module_line_count_check.py`)
- `infrastructure/validation/output/pipeline.py` (810 lines),
  `infrastructure/validation/rendered_snapshot.py` (800),
  `projects/templates/template_active_inference/src/orchestration/full_verification.py` (929).
- Status: deferred. These sit under the documented warn >=800 / fail >=950 gate with
  expiring downward-only ratchets; no threshold breached. Splitting them is polish,
  not debt.

**m2 — Local-only mirror-shape guard failures** (`scripts/audit/check_mirror_symlinks.py`)
- `projects/active/project`, `projects/working/Untitled`, `projects/working/ap3`
  are real directories/files rather than managed sidecar symlinks.
- Status: deferred — this is local workspace state, not tracked content
  (`check_tracked_all.py` confirms nothing offending is committed). Fix belongs to
  the workspace owner: move those trees into the sidecar repo and re-run
  `link-projects`.

**m3 — Bandit comment noise** — `[manager] WARNING: Test in comment ...` lines
during bandit runs are upstream bandit id-parsing noise against words like
"dev", "proxy", "pidfile" in comments. No findings; no action.

### Major (scoped, NOT implemented per mission)

**J1 — Oversized validation/rendering modules (architectural split)**
- Scope: `infrastructure/validation/output/pipeline.py` (~810 lines),
  `infrastructure/validation/rendered_snapshot.py` (~800 lines).
- Approach: extract stage-level helpers into sibling modules behind the existing
  public API; keep `__all__` stable (the `check-all-exports` gate pins exports);
  move corresponding tests incrementally.
- Effort: 2–3 days including gate reruns (`health`, drift, line-count ratchet update).
- Risks: dotted diagnostic-ID stability (`validation/content/diagnostic_codes.py`)
  must not shift; downstream `jq` filters depend on them.
- Acceptance: all current gates green; module sizes below the 800-line warning
  threshold; no public-API rename.

## Concurrent-writer note (important)

This checkout had **another active writer during the pass**. Files not listed in the
arrival snapshot appeared or changed mid-session: `docs/audit/filepath-audit-report.md`,
`docs/development/coverage-gaps.md`, several `template_active_inference` output/data
files, several `tests/infra_tests/rendering/*` files, and the untracked
`tests/infra_tests/project/test_workspace_branches.py` itself (created by that
writer, then repaired by me for M1). Per mission hard rules I did not revert, commit,
or further modify any of those beyond the minimal M1 repair inside
`test_workspace_branches.py`. That file is therefore **left uncommitted** here to
avoid a double-commit collision with the concurrent writer, who documents the same
file in `coverage-gaps.md`. The M1 verification evidence above stands regardless of
who lands the file.

## Deliverable checklist

- [x] This report at repo root with classified findings + scoping
- [x] Enforced-gate Medium finding fixed and verified (M1); minors explicitly dispositioned
- [x] Path-scoped commit of my own file (this report only — M1 file intentionally
      left uncommitted, reason above)
- [ ] No push (per mission)
