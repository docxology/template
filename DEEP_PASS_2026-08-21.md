# DEEP_PASS_2026-08-21 — Deep Assessment & Improvement Pass

Scope: read-only deep assessment of this repository plus implementation of any
Minor/Medium fixes that could be completed and verified within the session budget.
Per mission rules, no pre-existing uncommitted work was touched, reverted, or committed.

## Executive summary

Repository health is **excellent**. This is an unusually heavily-gated research
template monorepo with layered enforcement (pre-commit, pre-push, CI with 16 jobs,
generated-fact receipts with `--check` gates). Measured gate results from this pass:

| Gate | Command | Result |
| --- | --- | --- |
| Confidentiality guards | `scripts/audit/check_tracked_all.py` | clean (projects/fonds/rules/tools) |
| No-mocks lexical + semantic inventory | `verify_no_mocks.py --inventory --max-dependency-replacements 0` | clear; dependency_replacement = 0 |
| Ruff lint (public surface) | `public_scope lint-paths` + `ruff check .` | All checks passed |
| mypy (import-safe source paths, 1559 files) | `mypy $(... source-paths)` | Success: no issues |
| Bandit | `bandit -c bandit.yaml -r infrastructure scripts` | No issues identified |
| Tracked-secrets scan | `check_tracked_secrets.py` | No high-confidence credentials |
| Template drift (`--strict`) | `check_template_drift.py` | no drift detected |
| Backlog contract | `check_backlog.py` | 25 files, 22 stable IDs, 0 errors/warnings |
| Claim bindings | `check_claim_bindings.py` | status=pass (24 projects) |
| Public template contract | `check_public_template_contract.py` | pass, 0 findings |
| Generated docs checks | docgen `--check`, `infrastructure.skills check`, `check-contracts` | all exit 0 |
| Infra tests (project/ subtree) | `pytest tests/infra_tests/project -q` | **573 passed**, 32 deselected by default markers, exit 0 (~2.5 min serial) |
| Filepath/reference audit | `audit_filepaths.py` | 0 actionable issues; 55 link issues, all classified known exceptions/false positives (report regenerated this pass) |
| Manuscript prerender pre-flight | `infrastructure.validation.cli prerender` (template_code_project) | no render-blocking pitfalls |

Two environment-only failures were root-caused and are NOT repo defects:
mermaid rendering (missing local Chrome for mmdc) and one pytest run that hung while
a concurrent session was rewriting tracked sources mid-run (re-ran clean afterwards).

## Findings

### Minor

1. **Stale generated filepath-audit snapshot.**
   Evidence: `docs/audit/filepath-audit-report.md` (generated timestamp older than
   current tree state).
   Status: **fixed** — regenerated via the canonical producer
   (`uv run python scripts/audit/audit_filepaths.py`; the directory AGENTS.md forbids
   hand-editing). Verified: fresh generation stamp, 2062 files scanned, 0 actionable
   issues.

2. **Local-environment mermaid lint failures (docs-lint exit 1 locally).**
   Evidence: all 8 failures in `.github/README.md` / `.github/AGENTS.md` are `mmdc`
   exit-124 timeouts; direct reproduction shows
   `Error: Could not find Chrome (ver. 131.0.6778.204)` under `/Users/4d/.cache/puppeteer`.
   The mermaid blocks themselves parse fine.
   Status: **deferred (not a repo defect)** — CI's ubuntu runner provisions Chrome, so
   `docs-lint` passes there. Local remediation is
   `npx puppeteer browsers install chrome-headless-shell`; machine state, not repo content.

3. **Outdated transitive/dev dependencies available on PyPI.**
   Evidence: `uv pip list --outdated`: bandit 1.9.3→1.9.4, hypothesis →6.165.x,
   jax 0.9.2→0.11.x, inferactively-pymdp 1.0.1→1.0.3, etc.
   Status: **deferred** — updates are pinned via `uv.lock` and belong to the Dependabot
   channel (`.github/dependabot.yml` + automerge workflow); hand-bumping would bypass
   review across the Python 3.10–3.14 test matrices.

4. **29 TODO/FIXME/XXX mentions under `infrastructure/`+`scripts/` (non-test).**
   Status: **verified non-issue (no fix needed)** — every hit is either a docstring
   reference to the TODO/backlog system itself or code inside the security-TODO scanner
   (`infrastructure/project/promotion/security_gate.py`). Zero unresolved engineering
   TODOs; `check_backlog.py` reports 0 errors / 0 warnings, 22 stable identifiers.

5. **One-shot pytest hang under concurrent modification of tracked sources.**
   Evidence: first `pytest tests/infra_tests/project` run emitted a selectors.Timeout
   traceback; identical rerun on a quiet tree passed 573/573 (exit 0, ~156s serial).
   Root cause: git-guard tests read the real tree while another session rewrote tracked
   outputs concurrently.
   Status: **documented, no code change** — matches the documented serial-only warning
   in `tests/AGENTS.md`. Not reproducible on a quiet tree.

### Medium

None found actionable within scope. Two candidates observed were traced to transient /
concurrent-session artifacts: a transient "dependency replacement = 2" reading during
the health registry run re-measured as 0 once the tree was quiet; ruff-format flags on
arrival-dirty files belong to another in-flight change set that this pass must not touch.

### Major (scoped, not implemented)

M-1. **Concurrent-session safety for real-tree gate tests.**
   Evidence: `tests/infra_tests/project/test_git_guards.py`,
   `test_private_project_promotion.py`, and the autouse restore fixture in
   `projects/templates/template_active_inference/tests/conftest.py` read or rewrite
   tracked sources in-place; a second writer in the same checkout corrupts runs
   (hang observed above; `-n 6` corruption note in active_inference tests/AGENTS.md).
   Approach: repo-level advisory exclusive lock (flock acquired by a conftest session
   fixture in `tests/conftest.py` and each public exemplar suite), or copy-on-enter
   isolation following the disposable-tree pattern already used by
   `scripts/docgen/counts.py` coverage measurement. Effort: 2–4 days including
   negative controls.
   Risks: lock contention changes local UX (second run waits or fails fast instead of
   racing); copy-isolation increases disk usage and may break tests asserting
   repo-relative paths.
   Acceptance criteria: two simultaneous pytest invocations on one checkout both pass
   (or one fails fast with a clear message); full infra suite green serially; zero new
   dependency-replacement debt per `verify_no_mocks.py --inventory`.

## What was intentionally not done

- No commits touching the pre-existing dirty set (rendering pipeline work, active
  inference output regeneration, CHANGELOG, health/matrix edits, untracked test files
  — all belong to concurrent sessions).
- No pushes (per mission rules).

## Verification log (measured, this session)

- `git branch --show-current` → main (not detached).
- Arrival dirty set: 5 modified files; grew to 20+ during the session (concurrent
  sessions actively editing); none touched by this pass except items credited above.
- Every gate command in the summary table was executed live this session with the
  recorded exit codes; nothing inferred or fabricated.
