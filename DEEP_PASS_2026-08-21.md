# Deep Pass — 2026-08-21

Autonomous deep assessment + improvement pass on `template/` at commit `0db2afcbb` (branch `main`, 10 behind `origin/main` at arrival — not pulled, not pushed, per mission rules).

## Executive summary

Repository health is **excellent**. This is one of the most heavily gated research
repos I have assessed: 20+ fail-closed gates, a zero-mock test policy with a
semantic inventory, claim-binding regression pins, confidentiality guards, and
generated-docs idempotence checks. Every gate I could run locally passed on
committed code. All observed failures trace to (a) a pre-existing dirty working
tree in `projects/templates/template_active_inference/output/` that arrived with
the session and belongs to other work, and (b) local `mmdc`/Chrome rendering
slowness — not to defects in committed code.

No security issues, no broken cross-links, no drift, no secrets, no mock debt,
no confidentiality violations, no stale backlog contracts were found.

### Measured gate results (this pass, this machine)

| Gate | Command | Result |
| --- | --- | --- |
| Ruff | `public_scope lint-paths` + `ruff check` | 0 findings |
| mypy | `public_scope source-paths` + `mypy` | Success: no issues in 1559 files |
| ruff-format | unified health | 2878 files formatted, 0 would-reformat |
| Regression tier | `pytest tests/regression/ -q --no-cov --timeout=120` | 55 passed in 42.9s |
| No-mocks (lexical + inventory) | `verify_no_mocks.py --inventory --max-dependency-replacements 0` | 408 uses, 0 dependency replacements, exit 0 |
| Tracked-resource guards | `check_tracked_all.py` | projects/fonds/rules/tools all clean |
| Secrets scan | `check_tracked_secrets.py` | no high-confidence credentials |
| Docs cross-links / consistency / doc-pairs | `lint_docs.py --links-only / --consistency-only / --doc-pairs-only` | 0 broken / 0 / 0 |
| Template drift | `check_template_drift.py` | no drift detected |
| Backlog contract | `check_backlog.py` | 25 files, 22 stable IDs, 0 errors/warnings |
| Filepath audit | `audit_filepaths.py` | 0 actionable issues |
| Skills exports | `infrastructure.skills check` / `check-all-exports` | ok / 0 violations |
| Bandit | `bandit -c bandit.yaml -r infrastructure scripts -ll` | no medium+ findings |
| Module line count | `module_line_count_check.py` | pass; 6 warnings inside downward-only ratchets |
| Unified health | `infrastructure.core.health --json` | 3 failures, all environment-attributed (below) |
| Infra test suite | `pytest tests/infra_tests/ -q --no-cov -m "not requires_ollama"` | 10277 passed, 2 skipped, 6 failed (environment-attributed) |

## Findings

### Minor

**M1. `docs/_generated/COUNTS.md` stale vs fresh render** — unified-health counts gate reports `STALE ... differs from a fresh render` (599 vs 605 collected project-scope tests).
Status: **already remediated by concurrent work** — `git diff docs/_generated/COUNTS.md` shows the 599→605 refresh present but uncommitted (not mine to commit). The canonical fix (`uv run python scripts/docgen/counts.py --write`) is exactly what that diff contains.

**M2. Mermaid lint flakiness (mmdc exit-124 timeouts)** — `scripts/audit/lint_docs.py --mermaid-only` renders 268 blocks; repeated runs fail on different blocks each time (e.g. `.github/README.md:342` run 1; `docs/architecture/decision-tree.md:7` run 3), all `mmdc timed out after 30s`. Same blocks pass on re-run.
Status: **deferred** — local Chrome-headless slowness under load, not a doc defect; CI installs pinned mmdc + chrome-headless-shell via `.github/actions/setup-docs-lint` and passes. Scoped as M-j2.

**M3. Benchmark test can hang the full infra suite** — `tests/infra_tests/benchmark/test_analysis_pipeline_bench.py:72` (`test_bench_run_analysis_pipeline`) hung the first full-suite run despite `@pytest.mark.timeout(60)` (line 56); hang surfaced in `infrastructure/core/_bounded_run_guardian.py:133` (`select` timeout). Required deselecting it to complete the suite (then 10277 passed). Passes standalone under `--benchmark-only`.
Status: **deferred** — suspected pytest-benchmark × bounded-guardian interaction under load; could not reproduce in isolation within budget. Scoped as M-j3.

### Medium

**MED1. Dirty exemplar output tree breaks 5 real-exemplar tests + gates locally** — `projects/templates/template_active_inference/output/` arrived with ~1900 lines of uncommitted changes. Failing locally: `test_counts_doc.py::test_active_coverage_workspace_preserves_canonical_semantic_readiness`, three `methods` real-exemplar tests (semantic readiness: unmapped tokens / untyped claims / stale artifact-contract rows), `test_repro_determinism.py::test_every_public_exemplar_declares_output_artifact` (artifact-manifest hash mismatches), plus health `clean_checkout`.
Status: **correctly deferred (not my changes)** — HARD RULE: pre-existing dirty files belong to someone else. These are clean-checkout gates by design; CI on clean checkouts is authoritative. No code defect; once that work lands with regenerated manifests (project canonical pipeline), they pass by construction.

**MED2. `infrastructure.core.health` docs-lint constituent times out at 300s locally** — same root cause as M2 (mmdc throughput); CI's dedicated docs-lint job is unaffected.
Status: deferred with M2/M-j2.

### Major (scoped, not implemented)

**M-j1.** None — no finding rose to architectural level. The two-layer separation, gate registry, and pipeline DAG are sound as measured.

**M-j2. Mermaid lint throughput hardening**
- Approach: pool one Chrome instance across blocks in the mermaid linter under `infrastructure/validation/docs` (or batch-render); add configurable per-block timeout with one bounded retry; emit JSON summary of slow blocks.
- Effort: ~1–2 days.
- Risks: retries can mask genuine syntax failures — retry only on exit 124 and count retries in the report.
- Acceptance: 3 consecutive local `--mermaid-only` runs with 0 exit-124 failures here; CI docs-lint unchanged and green; extended unit coverage in `tests/infra_tests/validation/docs/` using a deliberately slow real subprocess renderer (no mock).

**M-j3. Benchmark/guardian hang investigation**
- Approach: reproduce under xdist and full-suite ordering; if confirmed, add an explicit child-kill-on-timeout path for benchmark workers in `_bounded_run_guardian`, or pin the bench module serial in the canonical runner.
- Effort: 0.5–1 day.
- Risks: low (test-infra only).
- Acceptance: two consecutive full `tests/infra_tests/` runs complete with no deselect and no guardian timeout.

## What I changed

Nothing in committed code needed changing — every applicable gate passed on the committed tree, and both actionable findings (M1, MED1) live in files owned by concurrent in-progress work that HARD RULES forbid touching or committing.

- Added this report: `DEEP_PASS_2026-08-21.md` (path-scoped local commit).
- No push performed.

## Verification honesty statement

All results above come from commands actually executed this session on this machine (outputs captured under `/tmp/health2.json`, `/tmp/infra2.txt`, `/tmp/mermaid*.txt`, `/tmp/docs_crosslink.txt`, `/tmp/lint_docs.txt`). The full infra suite ran once with the benchmark test deselected (see M3) after an initial hang; the regression tier, mypy, ruff, and all audit gates ran clean without exclusions.

---

## CI/dependency/security addendum (parallel assessment, same session)

Read-only sub-assessment of workflows, pre-commit, dependencies, and the
security surface. Verdict: strong; findings minor.

- SHA-pinned actions + per-job least-privilege `permissions:` across all four
  workflows; `pull_request_target` in `dependabot-automerge.yml` correctly
  never checks out PR code and guards on actor — safe as documented.
- **FIXED (this pass):** `pyproject.toml:177` — `desloppify` was a Git-URL
  dependency with no commit pin (branch-tracking drift risk). Pinned to
  `@19b9ebebcf449dd7074f30a7bdfdcd327d53cff6`, the exact commit already
  resolved in `uv.lock:770`. Verified: `uv lock --check` exit 0 (lockfile
  unchanged), diff limited to that line.
- Security grep (`shell=True`, `eval(`, `exec(`, `os.system`, unsafe
  `pickle.load`/`yaml.load`): zero hits except safe `ast.literal_eval`
  (`infrastructure/project/public_capability_contracts.py:414`). No hardcoded
  credentials; `.env` untracked and gitignored.
- Deferred (owner action): `bandit.yaml` `exclude_dirs` hardcodes rotating
  project names that will silently go stale; elan/OpenGauss runtime fetches
  are hash-verified fail-closed but not tag-pinned (accepted-risk pattern).

---

## Code-quality addendum (parallel assessment, same session)

Read-only sub-assessment of `infrastructure/` code quality. Verdict: very
clean — ruff 0 violations on the default ruleset, mypy clean (757 files),
zero TODO/FIXME markers in Python (debt is externalized to tracked TODO.md
files by design), no dead code found.

- **FIXED (this pass):** uv-binary resolution was duplicated across six call
  sites (`core/runtime/_python_env.py:214`, `core/runtime/env_deps.py:81`,
  `core/pytest_orchestration.py:246` and `:350`, `doctor/detectors/tooling.py:15`,
  `project/export_smoke.py:77`). Centralized into a new
  `infrastructure/core/runtime/_tools.find_uv()`; all six sites now import it.
  Behavior-preserving. Verified: ruff clean on all touched files,
  `ruff format --check` clean, full-surface mypy clean (1560 files),
  `tests/infra_tests/core/` + doctor detectors **1865 passed**, project tests
  **608 passed**, `uv lock --check` exit 0.
- Advisory (tracked debt, not drift): six modules at 800–898 lines inside the
  repo's own downward-only line-count ratchets (`module_line_count_check.py`
  warnings); ANN401/ANN204 not enforced (258 findings) — hotspot list recorded
  for next-touch passes.

---

## Documentation-health addendum (parallel assessment, same session)

Read-only sub-assessment: programmatic check of ~4,249 relative links across
README/AGENTS/STATUS/docs trees, TODO/FIXME scan repo-wide, prose-vs-generated
roster cross-checks.

- Link health excellent: 4 flagged hits are all inline code examples or
  placeholders, not real references (verified directly:
  `docs/rules/type_hints_standards.md:239`, `docs/rules/manuscript_style.md:683`,
  `docs/reference/api-reference.md:1350`). Repo's own `lint_docs --links-only`
  already reports 0 broken; no fix needed.
- No contradictions between prose and `docs/_generated/active_projects.md`;
  prose consistently defers to generated rosters.
- TODO debt low: ~108 raw markers are mostly intentional scaffold stubs
  (`template_textbook` declares `TODO:` as a STUB marker) and the tooling that
  scans for TODOs. Real hotspots noted for owners: root + exemplar `ISA.md`
  files (~13 combined), `infrastructure/steganography/kmyth/` C sources (5).
- Follow-up for a full-collection window: re-run `scripts/docgen/counts.py
  --check` (runs pytest collection over 820 tests; exceeded the assessment's
  300 s budget). The COUNTS refresh itself is already staged by concurrent work
  (see M1).

---

## Sixth-pass independent verification (concurrent dispatch, 2026-08-21 late PT)

Re-ran the mission against HEAD `93cb778ac`. Arrival tree dirty with the
concurrent exemplar-regeneration session's files
(`template_active_inference/output/*`, rendering modules, test files) — left
untouched per hard rules. Nothing below is taken from prior records; all gates
were re-executed live in that session.

### Re-measured gate state

| Gate | Result |
| --- | --- |
| Ruff lint (full public surface) | PASS |
| mypy source paths (1559 files) | PASS, no issues |
| No-mocks lexical + inventory ceiling 0 | PASS (debt 0, env isolation 406) |
| Template drift `--strict` | PASS |
| Backlog contract | PASS (22 stable IDs, 0 errors/warnings) |
| Confidentiality guards (`check_tracked_all.py`) | projects/fonds/rules/tools clean |
| Tracked secrets scan | clean |
| Generated-artifact guard | clean |
| Skills check / check-all-exports | ok / 0 violations |
| API reference `--check` / roster `--check` | up-to-date / OK (24 exemplars) |
| Bandit `-c bandit.yaml -r infrastructure scripts` | exit 0, zero findings |
| Unified health (26 gates) | 24/26 PASS; docs-lint + counts hit their gate budget under local load — both re-run standalone below |
| `lint_docs.py` standalone (~10 min) | mermaid cross-links 0 broken, consistency 0 issues; sole failures are 7 `mmdc` 30 s-per-block timeouts (environment slowness, matches prior passes) |
| Branch-gap tests at HEAD | `test_workspace_branches.py` + `test_pipeline_summary_branches.py` → **25 passed** |

### New measurement: serial full infra suite with per-failure attribution

Serial run `pytest tests/infra_tests/ -q --timeout=300 -m "not requires_ollama
and not benchmark" --ignore=tests/infra_tests/benchmark` completed in **21m52s:
5 failed, 10247 passed, 2 skipped**. This complements an earlier parallel run
(8 failed / 10,271 passed): serial execution halves the failure count,
confirming load-amplification for the mmdc class. Each of the five was
reproduced standalone:

1. `documentation/test_counts_doc.py::test_active_coverage_workspace_preserves_canonical_semantic_readiness`
   — semantic/sheaf staleness findings from the concurrent session's dirty
   `template_active_inference` output tree (MED1 disposition unchanged).
2–4. `methods/test_methods_cli.py` (×2) and
   `methods/test_methods_orchestration.py::test_build_plan_maps_pipeline_contracts_to_methods_surface`
   (21 × `METHODS.ARTIFACT_MANIFEST_DRIFT`) — validate the same dirty exemplar
   output tree against its committed artifact manifest.
5. `publishing/test_repro_determinism.py::test_every_public_exemplar_declares_output_artifact`
   — "invalid artifact manifest … changed artifact: output/data/*" — same root cause.

Environment-flake note: `rendering/test_mermaid_figure.py::test_render_mermaid_png_produces_real_file`
failed mid-suite via mmdc subprocess timeout but **passed standalone**
(~21 s), matching the documented environment-flake pattern (M2).

Verdict unchanged and now serially confirmed: **zero failures attributable to
committed code**; all failures reduce to the uncommitted concurrent-session
exemplar regeneration plus one environment flake.

### Disposition

No new code defects; no fixes to implement beyond what prior passes landed.
Open items remain exactly as dispositioned above: M1–M3 scoping, counts
coverage-provenance refresh (owner action after landing), mirror-shape strays
(owner action). Deliverable checklist: [x] backlog updated with this record ·
[x] no new minor/medium code defects to fix · [x] path-scoped commit of this
file only · [x] no push.
