# Deep Pass — 2026-08-21

> Note: a prior autonomous session's same-day deep-pass report was previously committed
> at this filename; it is preserved verbatim at `DEEP_PASS_2026-08-21_prior_session.md`.
> This file is the second same-day pass (this session's assessment + backlog).

## Executive summary

Repository health is strong. On this checkout (main @ 0db2afcbb, dirty on arrival
with a concurrent writer's in-flight rendering/health work):

- Ruff clean across the full public lint surface; **mypy strict clean over 1,559 source files**.
- Confidentiality guards (`check_tracked_all`): projects/fonds/rules/tools all clean.
- Secrets: no high-confidence credentials in tracked blobs; staged-scan contract tested.
- No-mocks lexical gate PASS; semantic inventory within the zero-dependency-replacement ceiling.
- Backlog contract (25 files, 22 stable IDs): 0 errors, 0 warnings. Claim bindings pass (24 projects).
- Public template contract pass (24 exemplars, 0 findings). Exemplar roster, API reference,
  stage table, skills/operations manifests, drift checks: all in sync.
- Two flaky smoke tests found and fixed (timeout caps tighter than their own documented
  runtimes); details below.
- Three unified-health failures triaged: two attributable to the concurrent dirty tree or
  local environment, one genuine staleness item deferred pending settlement of the
  concurrent work.

No security issues, no broken public contracts, no architectural defects introduced.
Findings below are classified Minor / Medium / Major per mission scope.

## Findings

### Minor

| # | Finding | Evidence | Status |
|---|---------|----------|--------|
| M1 | Duplicate stacked `@pytest.mark.timeout` markers (150/180) left from an edit on the generated-artifact guard smoke test | tests/infra_tests/git_hook_smoke/test_tracked_generated_artifacts.py:58-59 (pre-fix) | **Fixed**: single `@pytest.mark.timeout(240)` with subprocess cap 120s kept below the marker so failures report cleanly. Verified: `uv run pytest tests/infra_tests/git_hook_smoke/ -q --import-mode=importlib` -> 14 passed in 52.50s. *Not committed* — see note below. |
| M2 | Doc-pair lint CLI test used a 30s subprocess cap against a full-repo walk measuring ~23s — flaky under load (observed TimeoutExpired twice) | tests/infra_tests/validation/docs/test_doc_pair_lint.py:149 (pre-fix) | **Fixed in working tree** (cap raised; a concurrent writer bumped it independently during this pass). Verified: test passes standalone (15.04s) and the underlying CLI exits 0 with empty diagnostics (~23s measured). |
| M3 | Mermaid blocks reported failing by docs lint | .github/README.md:342+; docs/architecture/two-layer-architecture.md:39 | **Not a doc defect** — every failure is mmdc exit 124 (renderer timeout under local load), including on blocks that render fine on unloaded runs. Cross-link, consistency, and doc-pair linters all report zero issues. No change made. |

### Medium

| # | Finding | Evidence | Status |
|---|---------|----------|--------|
| D1 | `counts.py --check` fails: stale coverage provenance for template_active_inference ("source hash changed") | scripts/docgen/counts.py --check output; docs/_generated/coverage_snapshot.json | **Deferred**: hash drift comes from the concurrent writer's uncommitted edits to that project's sources. Refreshing provenance now would snapshot unsettled work. Follow-up once it lands: rerun the project coverage gate, then `uv run python scripts/docgen/counts.py --refresh-coverage-provenance --write`. An attempt during this pass confirmed the project suite does not settle while edits are in flight (claim-ledger validation fails mid-run). |
| D2 | `ruff-format` would reformat 2 files | infrastructure/core/health.py; tests/infra_tests/rendering/test_slides_renderer_core.py | **Deferred**: both files carry the concurrent writer's uncommitted changes; formatting them would entangle that work. Trivial follow-up (`uv run ruff format <paths>`) after it settles. |
| D3 | Outdated dependency pins visible via `uv pip list --outdated` (bandit 1.9.3->1.9.4, inferactively-pymdp 1.0.1->1.0.3, jax 0.9.2->0.11.1, etc.) | uv.lock / pyproject.toml | **Deferred**: lockfile-managed; uv.lock is currently dirty from the concurrent writer. Dependabot already covers this lane (.github/README.md). |

### Major (scoped, not implemented)

None new. The two known warn-level oversized modules remain on their expiring
downward-only ratchets (scripts/gates/module_line_count_check.py):
infrastructure/validation/rendered_snapshot.py (800 lines) and
projects/templates/template_active_inference/src/orchestration/full_verification.py
(929 lines). Both already sit on the existing ratchet mechanism with documented
expiry; splitting either is multi-day refactor work with real regression risk against
gate suites that exercise the real project tree serially. Existing scope lives in
TO-DO.md; no additional major finding surfaced.

## Verification performed (measured)

| Check | Result |
|---|---|
| `ruff check` (public lint surface) | All checks passed |
| `mypy` (public source paths, 1,559 files) | Success: no issues |
| `verify_no_mocks.py --inventory --max-dependency-replacements 0` | PASS |
| `check_tracked_all.py` | projects/fonds/rules/tools: clean |
| `check_tracked_secrets.py` | No findings |
| `check_tracked_generated_artifacts.py` | PASS |
| `check_template_drift.py --strict` | no drift detected |
| `skills check` / `check-all-exports` / operations manifest | OK |
| `exemplar_roster.py --check`, `api_reference.py --check`, stage table | OK |
| `check_backlog.py` | 25 files, 0 errors |
| `check_claim_bindings.py` | status=pass |
| `check_public_template_contract.py` | 24 exemplars, pass |
| `pytest tests/infra_tests/validation -q -x` | 95 passed, 1 failed -> root-caused to M2 timing; passes with adequate cap |
| `pytest tests/infra_tests/git_hook_smoke/ -q` | 14 passed (post-fix) |
| `lint_docs.py --doc-pairs-only --json` | exit 0, doc_pairs=[] |
| Unified health (`infrastructure.core.health`) | 23/26 gates PASS; 3 failures triaged above (M3 environment, D1/D2 concurrent-dirty-tree artifacts) |

## Commit policy note

The single code fix owned by this pass (M1) sits in a file that also carries the
concurrent writer's uncommitted timeout work. Per the mission's hard rule against
committing others' in-flight changes, that path was deliberately **not** committed;
the fix is live in the working tree and verified there. This report documents it for
inclusion in the next natural commit of that test module.

Nothing was pushed; no remote operations were performed.
