{
  "assessments": {
    "dependency_health": 62,
    "test_strategy": 58,
    "ai_generated_debt": 52,
    "package_organization": 55
  },
  "findings": [
    {
      "dimension": "dependency_health",
      "identifier": "requests_in_core_health_check",
      "summary": "requests imported lazily inside health check network probe, but requests is a hard runtime dep used only for an optional liveness ping to httpbin.org",
      "related_files": ["infrastructure/core/health_check.py"],
      "evidence": [
        "Line 172: `response = requests.get('https://httpbin.org/status/200', timeout=5)` inside `_check_network` — makes a real outbound HTTP call at health-check time",
        "requests is listed as a root `[project]` dependency (pyproject.toml line 42) even though the only infrastructure consumers are this health probe plus publishing/LLM modules that could carry it themselves"
      ],
      "suggestion": "Move the network liveness probe to an optional/best-effort helper that is not wired into the default health-check path; or gate it behind an explicit `network_check=True` flag so the hard dep on requests is justified by more than one optional call.",
      "confidence": "medium"
    },
    {
      "dimension": "dependency_health",
      "identifier": "scipy_unused_in_root_but_declared_in_sub_project",
      "summary": "scipy is declared only in projects/code_project/pyproject.toml yet the root workspace has no workspace members, so it is never hoisted; infrastructure code that checks dependencies (environment.py) does not include it, creating a silent gap",
      "related_files": ["projects/code_project/pyproject.toml", "pyproject.toml"],
      "evidence": [
        "projects/code_project/pyproject.toml declares `scipy>=1.10.0` but `[tool.uv.workspace] members = []` in root pyproject.toml means the sub-project is not a real workspace member",
        "infrastructure/core/environment.py default `required_packages` list does not include scipy, so a missing scipy is never caught at environment-check time"
      ],
      "suggestion": "Either add the sub-project as a proper uv workspace member, or move scipy into a `[dependency-groups]` entry in the root so it is managed alongside other optional deps.",
      "confidence": "medium"
    },
    {
      "dimension": "dependency_health",
      "identifier": "dev_group_duplicates_optional_group_entries",
      "summary": "python-dotenv and qrcode appear in both the optional `dotenv`/`steganography` dependency groups and in the `dev` group, creating redundant pinned entries",
      "related_files": ["pyproject.toml"],
      "evidence": [
        "pyproject.toml lines 66-67: `dotenv` group declares `python-dotenv>=1.0.0`",
        "pyproject.toml line 92: `dev` group independently declares `python-dotenv>=1.0.0`",
        "pyproject.toml lines 70-72: `steganography` group declares `qrcode[pil]>=8.2`; `dev` group line 93 independently declares `qrcode[pil]>=8.2`"
      ],
      "suggestion": "Remove the duplicate entries from the `dev` group and instead list the optional group names under `default-groups` or use `uv sync --group steganography --group dotenv` in CI, so there is a single source of truth for each dependency version constraint.",
      "confidence": "high"
    },
    {
      "dimension": "test_strategy",
      "identifier": "coverage_parser_and_coverage_reporter_have_no_tests",
      "summary": "infrastructure/reporting/coverage_parser.py and infrastructure/reporting/coverage_reporter.py are called from the main test runner script but have zero test files targeting them",
      "related_files": [
        "infrastructure/reporting/coverage_parser.py",
        "infrastructure/reporting/coverage_reporter.py",
        "scripts/01_run_tests.py"
      ],
      "evidence": [
        "Grep for 'coverage_parser|coverage_reporter' in tests/ returns no files",
        "scripts/01_run_tests.py line 39 imports from `infrastructure.reporting.coverage_parser`, making it a high-import critical path module",
        "infrastructure/reporting/ has 14 source files; only 10 corresponding test files exist in tests/infra_tests/reporting/"
      ],
      "suggestion": "Add test_coverage_parser.py and test_coverage_reporter.py that exercise `extract_failed_tests`, `check_cov_datafile_support`, and `parse_coverage_json` with synthetic pytest stdout strings and a temp JSON file.",
      "confidence": "high"
    },
    {
      "dimension": "test_strategy",
      "identifier": "time_sleep_in_tests_introduces_timing_fragility",
      "summary": "Multiple test files use time.sleep() to create measurable durations, coupling test correctness to wall-clock timing",
      "related_files": [
        "tests/infra_tests/core/test_progress.py",
        "tests/infra_tests/core/test_performance.py",
        "tests/infra_tests/core/test_logging_utils.py",
        "tests/infra_tests/core/test_security.py"
      ],
      "evidence": [
        "test_progress.py has 12+ time.sleep() calls ranging from 0.01s to 0.25s",
        "test_logging_utils.py line 229: `time.sleep(0.15)` to ensure duration > `min_duration_to_log` — couples test to the 0.1s threshold constant",
        "test_security.py line 332: `time.sleep(1.1)` — pushes beyond a rate-limit window, making the test brittle on slow CI"
      ],
      "suggestion": "Accept an injectable clock/time source in the production functions under test, or restructure the assertions to check relative ordering and result shape rather than elapsed wall time, eliminating the sleep dependency.",
      "confidence": "high"
    },
    {
      "dimension": "test_strategy",
      "identifier": "integration_tests_not_in_default_test_run",
      "summary": "tests/integration/ exists but is not included in the default pytest testpaths, so cross-module verification is silently skipped in normal runs",
      "related_files": ["pyproject.toml", "tests/integration/test_full_pipeline.py"],
      "evidence": [
        "pyproject.toml line 116: `testpaths = [\"tests\"]` — this should include integration/, but the directory was found via grep only in test_full_pipeline.py timing search",
        "The `test-infra` script entry in pyproject.toml explicitly passes `tests/infra_tests/` not `tests/`; `test-project` points to projects/; neither covers `tests/integration/`"
      ],
      "suggestion": "Either add `tests/integration` to `testpaths` (or include it in a named `integration` run in CI), or mark `test_full_pipeline.py` with `@pytest.mark.integration` and document how to run it, to make coverage of cross-module paths explicit.",
      "confidence": "medium"
    },
    {
      "dimension": "ai_generated_debt",
      "identifier": "bloated_docstrings_on_trivial_functions",
      "summary": "Several simple getter/forwarding functions carry multi-line docstrings with Args/Returns/Example sections that add no information beyond the function signature",
      "related_files": [
        "infrastructure/core/logging_utils.py",
        "infrastructure/core/environment.py"
      ],
      "evidence": [
        "logging_utils.py `get_logger` (lines 215-228): 13-line docstring for a 10-line function that calls `setup_logger`; the docstring restates what the type signature already says",
        "logging_utils.py `log_success` (lines 370-379): 10-line docstring including an Example block for a 3-line function that formats a string and calls `logger.info`",
        "environment.py `check_python_version` (lines 50-55): docstring says 'Verify Python 3.8+ is available. Returns: True if Python version is 3.8 or higher' — exact restatement of the function name and return type"
      ],
      "suggestion": "Trim docstrings on trivial functions to a single summary line. Reserve Args/Returns/Example sections for functions where parameter semantics or return shape are non-obvious.",
      "confidence": "high"
    },
    {
      "dimension": "ai_generated_debt",
      "identifier": "entry_exit_logging_on_every_utility_function",
      "summary": "Utility functions in environment.py and logging_utils.py emit logger.info() at the start of every function body, producing noisy logs that restate function names",
      "related_files": [
        "infrastructure/core/environment.py"
      ],
      "evidence": [
        "environment.py line 56: `logger.info('Checking Python version...')` — restates the function name",
        "environment.py line 79: `logger.info('Checking dependencies...')` — restates the function name",
        "environment.py line 195: `logger.info('Checking build tools...')` — restates the function name",
        "environment.py line 262: `logger.info('Setting up directory structure...')` — restates the function name",
        "environment.py line 424: `logger.info('Setting environment variables...')` — restates the function name"
      ],
      "suggestion": "Replace mandatory entry-log lines with `logger.debug()` calls or remove them entirely; callers in the pipeline already emit stage-level headers. Reserve `logger.info` for meaningful state changes (e.g., what was actually installed or what was missing).",
      "confidence": "high"
    },
    {
      "dimension": "ai_generated_debt",
      "identifier": "global_module_level_health_api_instantiation",
      "summary": "health_check.py instantiates a global HealthCheckAPI at module import time, performing side-effectful resource probes on every import",
      "related_files": ["infrastructure/core/health_check.py"],
      "evidence": [
        "Lines 319-320: `_health_api = HealthCheckAPI()` at module scope — `HealthCheckAPI.__init__` creates a `SystemHealthChecker` that stores `time.time()` immediately",
        "Three module-level wrapper functions (`quick_health_check`, `get_health_status`, `get_health_metrics`, lines 328-344) simply forward to `_health_api`, adding no logic — classic pass-through wrapper pattern",
        "The `HealthCheckAPI` class itself wraps `SystemHealthChecker` with only forwarding methods (`get_status`, `get_status_summary`, `is_healthy`, `get_metrics`) — two layers of single-implementation wrappers with no added policy"
      ],
      "suggestion": "Remove the global `_health_api` instance and the redundant `HealthCheckAPI` wrapper; expose `SystemHealthChecker` directly or use a module-level lazy factory. Eliminate the three pass-through module functions.",
      "confidence": "high"
    },
    {
      "dimension": "package_organization",
      "identifier": "validation_package_mixes_doc_audit_and_output_validation_concerns",
      "summary": "infrastructure/validation/ contains 22 files spanning at least three distinct concern groups — PDF/markdown validation, documentation auditing (doc_scanner, doc_quality, doc_accuracy, doc_completeness, repo_scanner, audit_orchestrator), and output structure checking — with no sub-package separation",
      "related_files": [
        "infrastructure/validation/__init__.py",
        "infrastructure/validation/audit_orchestrator.py",
        "infrastructure/validation/doc_accuracy.py",
        "infrastructure/validation/doc_completeness.py",
        "infrastructure/validation/doc_discovery.py",
        "infrastructure/validation/doc_models.py",
        "infrastructure/validation/doc_quality.py",
        "infrastructure/validation/doc_scanner.py",
        "infrastructure/validation/repo_scanner.py",
        "infrastructure/validation/issue_categorizer.py",
        "infrastructure/validation/output_validator.py",
        "infrastructure/validation/pdf_validator.py",
        "infrastructure/validation/markdown_validator.py"
      ],
      "evidence": [
        "22 files in a flat directory (Glob result shows no sub-directories)",
        "doc_* group (7 files: doc_models, doc_scanner, doc_discovery, doc_accuracy, doc_completeness, doc_quality, doc_models) has high intra-group coupling but lives alongside unrelated pdf_validator and output_validator",
        "issue_categorizer.py and audit_orchestrator.py are orchestration/categorization concerns distinct from both document scanning and PDF/markdown validation, yet share the flat namespace"
      ],
      "suggestion": "Create sub-packages infrastructure/validation/doc_audit/ (doc_models, doc_scanner, doc_discovery, doc_accuracy, doc_completeness, doc_quality, audit_orchestrator, issue_categorizer, repo_scanner) and infrastructure/validation/output/ (output_validator, pdf_validator, markdown_validator, figure_validator), leaving cli.py and integrity.py at the validation/ level.",
      "confidence": "high"
    },
    {
      "dimension": "package_organization",
      "identifier": "reporting_modules_not_exported_from_init",
      "summary": "infrastructure/reporting/ has four modules (coverage_parser.py, coverage_reporter.py, test_summary_generator.py, summary_generator.py, manuscript_overview.py) that are NOT re-exported from __init__.py, creating an inconsistent public surface",
      "related_files": [
        "infrastructure/reporting/__init__.py",
        "infrastructure/reporting/coverage_parser.py",
        "infrastructure/reporting/coverage_reporter.py",
        "infrastructure/reporting/test_summary_generator.py",
        "infrastructure/reporting/summary_generator.py",
        "infrastructure/reporting/manuscript_overview.py"
      ],
      "evidence": [
        "infrastructure/reporting/__init__.py exports from pipeline_reporter, error_aggregator, executive_reporter, output_reporter, output_organizer, multi_project_reporter, dashboard_generator — but not from coverage_parser, coverage_reporter, test_summary_generator, summary_generator, or manuscript_overview",
        "scripts/01_run_tests.py imports `from infrastructure.reporting.coverage_parser import ...` — bypassing the package's declared public surface",
        "tests/infra_tests/reporting/test_manuscript_overview.py and test_summary_generator.py import their modules directly, consistent with the __init__ omission"
      ],
      "suggestion": "Either add the missing modules to __init__.py __all__ to complete the public surface, or explicitly document (e.g. in the module docstring) that these modules are internal and intended for direct import only. Inconsistency between the two patterns increases navigation cost.",
      "confidence": "medium"
    }
  ]
}
