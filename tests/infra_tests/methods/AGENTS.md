# Methods Tests

Tests for `infrastructure.methods`, the Layer-1 methods orchestration contract.

Keep tests deterministic and filesystem-backed. Do not use mocks for pipeline,
project, or manuscript surfaces.
