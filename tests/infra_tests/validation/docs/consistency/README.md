# Documentation consistency lint tests

Pytest suite for `infrastructure.validation.docs.consistency_lint` checks
(README file inventories, shell contracts, import resolution, ghost paths, package counts).

Run:

```bash
uv run pytest tests/infra_tests/validation/docs/consistency/ -q
```
