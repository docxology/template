# Regression-oracle tests

Tests for the checked-in regression manifest, minimum collection floor, and
required-file fail-closed behavior. The claim-binding cases themselves live in
`tests/regression/`.
