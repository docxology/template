# SIA infrastructure tests

Regression tests for `infrastructure/sia/` — task layout, fixture replay loop,
evaluation runner, context ledger, and CLI helpers.

```bash
uv run pytest tests/infra_tests/sia/ -q
```
