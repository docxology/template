# `tests/regression/projects/template_eda_notebook/tables/` — agent guide

Read [`../AGENTS.md`](../AGENTS.md) first.

One test file per manuscript table or per group of tabular/text
quantitative claims. Load pins via the `load_pinned_values` fixture and
re-derive each value from `src/eda/*`; do not hardcode expected numbers.
