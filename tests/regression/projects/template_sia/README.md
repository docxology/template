# `tests/regression/projects/template_sia/` — regression suite

> Pinned numerical regression tests for `template_sia`
> manuscript values. Loads ground truth from
> [`../../pinned_values/template_sia.json`](../../pinned_values/template_sia.json).

## Layout

- `tables/` — one test file per manuscript table (currently the
  fixture-replay generation-metrics table and the self-improvement
  delta / final-token claims in `03_results.md`)

See [`../../README.md`](../../README.md) for the philosophy.
