# tables/ — agent guide

One test file per manuscript table (currently
`test_generation_metrics_claims.py`). Use the `pinned_values`
fixture; load values from `<claim_key>`. Provenance discipline
per [`../../../AGENTS.md`](../../../AGENTS.md).
