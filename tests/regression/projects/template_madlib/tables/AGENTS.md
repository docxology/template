# tables/ — agent guide

One test file per manuscript claim cluster (currently
`test_configuration_counts_claims.py`). Use the `pinned_values`
fixture; load values from `tables.<claim_id>`. Provenance discipline
per [`../../../AGENTS.md`](../../../AGENTS.md).
