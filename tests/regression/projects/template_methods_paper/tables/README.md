# `template_methods_paper` — table regression tests

> One file per manuscript table/section. Re-derives each value via the
> project's deterministic controlled-method DSL (`compile_method`,
> `run_all_gates`, `demo_chain_report`) and compares to the pinned
> ground truth in
> [`../../../pinned_values/template_methods_paper.json`](../../../pinned_values/template_methods_paper.json).
