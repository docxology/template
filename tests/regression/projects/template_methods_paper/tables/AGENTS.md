# tables/ — agent guide

One test file per manuscript table/section (currently
`test_compiled_plan_claims.py`). Use the `load_pinned_values`
fixture; load values from the per-key entries in
`pinned_values/template_methods_paper.json`. Provenance discipline
per [`../../../AGENTS.md`](../../../AGENTS.md).
