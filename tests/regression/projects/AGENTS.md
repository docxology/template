# `tests/regression/projects/` — agent guide

Read [`../AGENTS.md`](../AGENTS.md) first. This directory hosts
one subdirectory per project. Tests inside use the
`pinned_values(project_name)` fixture from `../../conftest.py`.
