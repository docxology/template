# tables/ — agent guide

One test file per manuscript table (e.g.
`test_word_count_table.py`). Use the `pinned_values` fixture;
load from `tables.<table_id>`. Provenance discipline per
[`../../../AGENTS.md`](../../../AGENTS.md).
