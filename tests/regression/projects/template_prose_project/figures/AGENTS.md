# figures/ — agent guide

Add one test file per manuscript figure (e.g.
`test_readability_metrics.py`). Use the `pinned_values` fixture.
Provenance discipline per
[`../../../AGENTS.md`](../../../AGENTS.md).
