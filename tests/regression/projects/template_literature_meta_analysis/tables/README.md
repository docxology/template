# `template_literature_meta_analysis` — table regression tests

> One file per manuscript results group. Re-derives each value **offline**
> from the project's committed deterministic seed corpus
> (`data/fixtures/modafinil_corpus.jsonl`) via the real bibliometric source
> functions and compares to the pinned ground truth in
> [`../../../pinned_values/template_literature_meta_analysis.json`](../../../pinned_values/template_literature_meta_analysis.json).
