# `tests/regression/projects/` — per-project regression suites

> One subdirectory per project. Each project's regression suite
> lives under `<project>/figures/` and `<project>/tables/`.

See [`../README.md`](../README.md) for the design.
