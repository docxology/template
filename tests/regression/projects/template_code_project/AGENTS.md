# `tests/regression/projects/template_code_project/` — agent guide

Read [`../../AGENTS.md`](../../AGENTS.md) first.

New regression tests for `template_code_project` go under
`figures/` or `tables/`. Use the `pinned_values` fixture; do not
hardcode expected numbers in test bodies.
