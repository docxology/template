# figures/ — agent guide

Add one test file per manuscript figure (e.g. `test_figure_3.py`).
Use the `pinned_values` fixture; load values from the
`figures.<figure_id>` group. Pair any pinned-value change with a
`_provenance` entry — see [`../../../AGENTS.md`](../../../AGENTS.md).
