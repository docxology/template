# `template_code_project` — figure regression tests

> One file per manuscript figure. Re-derives each figure's
> numerical values via the project's deterministic pipeline and
> compares to the pinned ground truth in
> [`../../../pinned_values/template_code_project.json`](../../../pinned_values/template_code_project.json).
