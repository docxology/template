# `template_code_project` — table regression tests

> One file per manuscript table. Re-derives each cell value via
> the project's deterministic pipeline and compares to the pinned
> ground truth in
> [`../../../pinned_values/template_code_project.json`](../../../pinned_values/template_code_project.json).
