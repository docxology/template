# tables/ — agent guide

One test file per manuscript table (e.g. `test_table_2.py`).
Use the `pinned_values` fixture; load values from
`tables.<table_id>`. Provenance discipline per
[`../../../AGENTS.md`](../../../AGENTS.md).
