# `template_gold_refinement` — table regression tests

> One file per manuscript surface. Re-derives each value via the
> project's deterministic refinery pipeline, formalism registry, and
> evidence assay, and compares to the pinned ground truth in
> [`../../../pinned_values/template_gold_refinement.json`](../../../pinned_values/template_gold_refinement.json).
