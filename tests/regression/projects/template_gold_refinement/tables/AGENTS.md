# tables/ — agent guide

One test file per manuscript surface (currently
`test_refinery_certification_claims.py`). Use the `pinned_values`
fixture; re-derive every value from the project's `src` package rather
than hardcoding it. Provenance discipline per
[`../../../AGENTS.md`](../../../AGENTS.md).
