# `tests/regression/projects/template_autoscientists/` — regression suite

> Pinned numerical regression tests for `template_autoscientists`
> manuscript values. Loads ground truth from
> [`../../pinned_values/template_autoscientists.json`](../../pinned_values/template_autoscientists.json).

## Layout

- `tables/` — one test file per manuscript table (currently the
  matched-budget comparison and per-mechanism ablation claims in
  `03_results.md`)

See [`../../README.md`](../../README.md) for the philosophy.
