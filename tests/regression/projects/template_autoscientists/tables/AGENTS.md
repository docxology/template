# tables/ — agent guide

One test file per manuscript table (currently
`test_coordination_results_claims.py`). Use the `pinned_values`
fixture; load values from `tables.<table_id>`. Provenance discipline
per [`../../../AGENTS.md`](../../../AGENTS.md).
