# `template_autoscientists` — table regression tests

> One file per manuscript table. Re-derives each cell value via
> the project's deterministic coordination-mechanism testbed and
> compares to the pinned ground truth in
> [`../../../pinned_values/template_autoscientists.json`](../../../pinned_values/template_autoscientists.json).
