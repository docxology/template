# tables/ — agent guide

One test file per manuscript claim group (currently
`test_introspection_metrics_claims.py`). Use the `pinned_values`
fixture; load values from the top-level pin keys. Provenance discipline
per [`../../../AGENTS.md`](../../../AGENTS.md).
