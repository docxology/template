# `template_template` — table regression tests

> One file per manuscript claim group. Re-derives each value via
> the project's live repository introspection functions and
> compares to the pinned ground truth in
> [`../../../pinned_values/template_template.json`](../../../pinned_values/template_template.json).
