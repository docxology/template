# Private project fixtures

Committed stubs for tests marked `private_project` when the real staging tree under `projects/working/` is absent.

## See also

- [`cogant/`](cogant/) — COGANT audit and coverage-table tool fixtures
